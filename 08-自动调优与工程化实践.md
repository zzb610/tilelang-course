# 第 08 章 自动调优与工程化实践

> **本章目标**：把一个已经正确的内核变成可重复调优、可验证归档的工程对象。先分清
> JIT 与显式编译入口，再建立受约束的配置空间，随后固定输入、校验结果、测量延迟，
> 最后保存最佳配置并说明它的适用范围。自动调优不会替你发明算法，它只会在给定空间
> 内选择配置。

**学习信息**

- 难度：中高级；预计用时：3–5 小时；
- 前置：第 05 章至少有一个正确 GEMM 基线；
- 运行范围：autotune 需要可用 GPU，完整性能对比以 CUDA 为主；
- 本章产出：受约束的 config 空间、固定输入的校验流程、最佳配置归档和性能报告。
- 参考：[Autotuning Guide](https://tilelang.com/programming_guides/autotuning.html)、
  [官方 autotune 示例](https://github.com/tile-ai/tilelang/tree/main/examples/gemm) 和
  [TileLang releases](https://github.com/tile-ai/tilelang/releases)。

**阅读路线：**先理解「一个工厂函数如何变成可调用 kernel」，再按四步执行 autotune：
生成候选、过滤非法配置、用同一批输入做正确性检查、用同一套计时方法选择配置。缓存和多后端
放在后面，因为它们解决的是复现和交付问题，不能替代前面的算法理解。

## 8.1 编译入口：`@jit` 与 `tilelang.compile`

| 入口 | 写法 | 适用 |
|---|---|---|
| `@tilelang.jit` | 工厂函数装饰器；首次调用从**实际张量**推断 target 与形状 | 模板化内核、原型 |
| `tilelang.compile(func, out_idx=[...], target="cuda")` | 显式指定 target 与输出参数 | 精确控制、库封装 |

```python
# jit 风格（第 01 章）：调用时给 N，其余编译参数烘焙进 IR
kernel = add(N, block=256, dtype='float32')

# compile 风格：out_idx 声明输出，调用免传输出张量
func = matmul(1024, 1024, 1024, 128, 128, 32)   # 返回 prim_func
jit_kernel = tilelang.compile(func, out_idx=[2], target="cuda")
c = jit_kernel(a, b)                              # 返回 [C]
```

`execution_backend`：`'auto' | 'tvm_ffi' | 'cython' | 'nvrtc' | 'torch'`（一般用
auto）。`pass_configs={...}` 可对编译传额外开关，例如：

```python
# 该装饰器打开一个版本敏感的编译开关；只在确认目标后端支持时启用。
@tilelang.jit(out_idx=[3], pass_configs={
    tilelang.PassConfigKey.TL_ENABLE_FAST_MATH: True,
})
def kernel(A, B, C):
    # A/B/C 的 shape 应在真实 prim_func 参数中显式声明；这里仅展示装饰器位置。
    ...
```

> `out_idx=[-1]` 表示最后一个参数是输出；多个输出时列出多个下标，调用返回元组。

## 8.2 装饰器式 autotune（最常用）

一个可用的调优空间必须同时满足三点：候选配置合法、所有候选使用同一批输入、每个候选
先通过正确性检查再进入计时。缺一项，得到的「最优」都可能只是测量流程的产物。

```python
# 生成调优候选；每个候选的 BM/BN/BK/stages/threads 都会进入编译配置。
import tilelang
import tilelang.language as T

def matmul_configs(M, N, K):
    # 这里的列表是搜索空间，不是实际 tensor；M/N/K 用于按问题尺寸裁剪候选。
    tiles = [64, 128]
    stages = [2, 3]
    threads = [128, 256]
    return [
        dict(block_M=BM, block_N=BN, block_K=BK, num_stages=S, threads=TH)
        for BM in tiles for BN in tiles
        for BK in [32, 64] for S in stages for TH in threads
    ]

@tilelang.autotune(configs=matmul_configs, warmup=25, rep=100, timeout=60)
@tilelang.jit(out_idx=[-1])
def matmul(M: int, N: int, K: int,
           block_M: int = 128, block_N: int = 128, block_K: int = 32,
           threads: int = 128, num_stages: int = 3,
           dtype: str = 'float16', accum_dtype: str = 'float32'):
    # 每个候选共享相同的输入 tensor shape：A=[M,K]、B=[K,N]、C=[M,N]。
    @T.prim_func
    def kernel(A: T.Tensor((M, K), dtype),
               B: T.Tensor((K, N), dtype),
               C: T.Tensor((M, N), dtype)):
        # 一个 block 负责 block_M×block_N 的 C tile。
        with T.Kernel(T.ceildiv(N, block_N), T.ceildiv(M, block_M),
                      threads=threads) as (bx, by):
            # A_s=[block_M,block_K]，B_s=[block_K,block_N]，C_f=[block_M,block_N]。
            A_s = T.alloc_shared((block_M, block_K), dtype)
            B_s = T.alloc_shared((block_K, block_N), dtype)
            C_f = T.alloc_fragment((block_M, block_N), accum_dtype)
            T.clear(C_f)
            for ko in T.Pipelined(T.ceildiv(K, block_K), num_stages=num_stages):
                # 每轮搬运一个 K tile，再累加到 C_f。
                T.copy(A[by * block_M, ko * block_K], A_s)
                T.copy(B[ko * block_K, bx * block_N], B_s)
                T.gemm(A_s, B_s, C_f)
            T.copy(C_f, C[by * block_M, bx * block_N])
    return kernel
```

使用要点：

- `configs` 可以是列表，也可以是 `(args...) -> list[dict]` 的可调用对象（可按问题
  尺寸裁剪空间、剔除非法配置）；
- 每个 dict 的键**必须等于**工厂函数里带默认值的参数名；
- 装饰器返回的可调用对象对同一参数元组通常只 autotune 一次，之后可复用缓存；具体
  缓存键和持久化范围以当前版本为准；
- 默认的输入供应方式（见 8.3）要求静态形状；动态形状请用 `set_autotune_inputs`。

## 8.3 输入供应与校验（autotune 的正确姿势）

```python
import torch
from tilelang.autotuner import set_autotune_inputs

M = N = K = 1024
A = torch.randn(M, K, device='cuda', dtype=torch.float16)   # shape: [M, K]
B = torch.randn(K, N, device='cuda', dtype=torch.float16)   # shape: [K, N]
C = torch.empty(M, N, device='cuda', dtype=torch.float16)   # shape: [M, N]

with set_autotune_inputs(A, B, C):        # 所有配置用同一批数据，公平对比
    tuned = matmul(M, N, K)               # 编译 → 校验 → 基准 → 选最优
    C = tuned(A, B)                       # out_idx=[-1] 时输出由 kernel 自动分配
```

| 机制 | 说明 |
|---|---|
| `set_autotune_inputs(A, B, C)` | 上下文管理器，固定输入（推荐） |
| `supply_type=TensorSupplyType.Auto` | 内置生成器：`Integer/Uniform/Normal/Randn/Zero/One`，按 dtype 启发式生成 |
| `supply_prog(signature)` | 自定义供应程序，返回张量列表（动态形状时最灵活） |
| `ref_prog(A, B, C)` | 参考程序，返回 bool 或抛异常，用于正确性判定 |
| `manual_check_prog(...)` | 备选校验入口：只读检查输出并自行抛异常，适合无法写成 bool 比较的场景 |
| `rtol/atol`（默认 1e-2）、`max_mismatched_ratio`（默认 1%） | 数值容差 |
| `skip_check=True` | 跳过校验（更快，慎用） |

```python
# 参考程序比较 C=[M,N] 与 fp32 矩阵乘结果，返回是否通过容差。
def ref_prog(A, B, C):
    # A=[M,K]、B=[K,N]、C=[M,N]；转 fp32 后再计算参考结果。
    return torch.allclose(C, (A.float() @ B.float()).to(C.dtype),
                          rtol=1e-2, atol=1e-2)
```

> 陷阱：float8 的可用 dtype 取决于 PyTorch、目标 GPU 和 TileLang 版本；符号维度
> 通常需要使用 `T.const` 的 eager/JIT 入口，并通过 (1)/(2) 提供具体输入，内置生成器不一定支持。
>
> 容差不是「默认 1e-2 走天下」。"Correct but Slow" 研究（[arxiv 2607.04454](https://arxiv.org/abs/2607.04454)）
> 的实测口径可供参考：fp32 用 1e-5、fp16 用 1e-3、bf16 用 1e-2，归约类再放宽 2 倍；
> 它还用 NaN/Inf/denormal/全相等四类数值边界做补充。若你的算子含归约或大 K 累加，
> 请显式写出自己的容差依据，而不是复制示例值。

## 8.4 编程式 Autotuner（精细控制）

```python
from tilelang.autotuner import AutoTuner

kernel_factory = matmul                              # 已 @tilelang.jit 的工厂
tuner = AutoTuner.from_kernel(kernel_factory(M, N, K),
                              configs=matmul_configs(M, N, K))
tuner.set_profile_args(
    warmup=25, rep=100, timeout=60,
    supply_type=tilelang.TensorSupplyType.Auto,
    ref_prog=lambda A, B, C: torch.allclose(
        C, (A.float() @ B.float()).to(C.dtype), rtol=1e-2, atol=1e-2),
)
tuner.set_compile_args(target='auto', execution_backend='auto',
                       out_idx=[-1], pass_configs={})
artifact = tuner.run()
best_kernel, best_latency, best_config = artifact.kernel, artifact.latency, artifact.config
```

### 8.4.1 大空间先减少浪费，再增加候选

当前官方示例提供了三类缩短 wall time 的机制：并行/分组编译、单配置超时和 early stop。
它们减少的是调优成本，不会扩大算法覆盖，也不会修复错误的参考实现。

```python
@tilelang.autotune(
    configs=matmul_configs(M, N, K),
    warmup=5,
    rep=50,
    early_stop=True,
    early_stop_factor=2.0,
)
@tilelang.jit(out_idx=[-1])
def matmul(...):
    ...
```

`early_stop_factor` 用当前观测到的较优结果筛掉明显慢的候选。阈值太激进可能提前终止仍有
价值的配置，因此要先在小空间中比较 `early_stop=False/True` 是否得到一致的最优区域。
编程式接口还可在 `run(...)` 时启用 grouped compile；具体参数以当前版本文档为准。

调优空间也可以包含每个候选自己的 `pass_configs`。这适合验证 warp specialization 等编译
策略，但必须把 pass 配置写入最终结果；只保存 BM/BN/BK 会丢失复现条件。

三个官方文档写明的实现细节，直接影响你调参的姿势（以 0.1.13 Autotuning Guide 为准）：

1. **configs 可以是按问题尺寸裁剪的函数**。例如「`min(M,N,K) >= 1024` 时只扫
   `tile=128`，否则扫 `[64, 128]`」，把搜索空间约束在合法且相关的子集上——这正是
   第 05 章 RC2a 教训（缺维度）的反面：空间要有针对性，而不是越大越好。
2. **`timeout` 由 daemon 线程实现，超时的 CUDA 调用无法被强杀**。超时配置的后台
   线程会继续跑到调用返回；批量调参时不要只依赖 timeout 兜底，先过滤非法配置。
3. **torch execution backend 可能不把产物落盘**（此时只有进程内缓存，会打 warning）；
   需要跨进程/CI 复用调优结果时，选会落盘的 backend 并做一次
   `save_to_disk` → `load_from_disk` 回读验证。

## 8.5 缓存机制：把实验结果变成可复现产物

- 编译缓存：默认 `~/.tilelang/cache`（`TILELANG_CACHE_DIR` 可改）；
- autotune 磁盘缓存：`$TILELANG_CACHE_DIR/autotuner`，键包含（版本、函数源码、
  闭包自由变量、configs、compile/profile args）；
- 调优产物通常包含 `best_config.json`、`latency.json`、生成源码与后端相关库文件。
  同一缓存键再次运行时可以复用结果，但反序列化、装载和后端限制仍可能产生开销；
- 环境变量速查：

| 变量 | 作用 |
|---|---|
| `TILELANG_CACHE_DIR` | 缓存根目录（默认 `~/.tilelang/cache`） |
| `TILELANG_TMP_DIR` | 临时目录（默认 `$CACHE/tmp`） |
| `TILELANG_DISABLE_CACHE=1` | 关闭全部内核缓存（调试用） |
| `TILELANG_AUTO_TUNING_DISABLE_CACHE=1` | 只关 autotune 磁盘缓存 |
| `TILELANG_AUTO_TUNING_CPU_UTILITIES` | 控制 autotune 可使用的 CPU 比例 |
| `TILELANG_AUTO_TUNING_CPU_COUNTS` / `_MAX_CPU_COUNT` | 控制并行编译/基准的 CPU 数 |

> 注意：缓存目录中的文件格式、可复用范围和执行后端能力会随版本变化；需要可移植
> 产物时，先用当前版本的 `AutotuneResult.save_to_disk/load_from_disk` 做一次回读验证，
> 不要把缓存目录当作跨版本 ABI。

## 8.6 手动扫参：`par_compile`

不想用完整 autotuner 时，parallel compile 一批配置自己 benchmark：

```python
impl = matmul   # JITImpl
cfgs = [
    dict(block_M=64, block_N=128, block_K=32),
    dict(block_M=128, block_N=128, block_K=64),
]
kernels = impl.par_compile(cfgs, num_workers=4)
for k in kernels:
    k(A, B, C)                     # 自己计时比较
```

## 8.7 沉淀最优配置

```python
# 保存已调优配置和编译产物，供后续 CI 或回归测试复用。
from tilelang.autotuner.param import AutotuneResult, CompileArgs
# artifact 代表当前 autotune 产生的结果对象。
artifact.save_to_disk('out/best/matmul_1024', verbose=True)
# 从磁盘恢复后仍需重新做正确性和性能验证。
restored = AutotuneResult.load_from_disk('out/best/matmul_1024', CompileArgs())
restored.kernel(A, B, C)
```

产物目录里的 JSON 适合 CI 归档和复盘，但不能自动保证跨 GPU、跨版本或跨驱动可执行。
恢复后仍要重新做一次正确性和基准验证，再决定是否把最佳配置固化成默认参数。

## 8.8 性能测量与报告

```python
profiler = kernel.get_profiler()
profiler.assert_allclose(ref_prog, rtol=0.01, atol=0.01)  # 顺便校验
lat = profiler.do_bench(warmup=25, rep=100)               # CUDA event 计时
lat_cupti = profiler.do_bench(backend='cupti')            # CPU 侧 + CUPTI（更长，更准）

tf = 2 * M * N * K / (lat * 1e-3) / 1e12
print(f"{tf:.1f} TFLOPS")
```

报告公式：`TFLOPS = 2·M·N·K / latency / 1e9`（latency 单位 ms）。

## 8.9 多后端与动态形状

```python
# target: 'auto' | 'cuda' | 'hip' | 'metal'
kernel = tilelang.compile(func, target='hip')     # AMD GPU（ROCm 构建）
```

动态形状：核心里用符号维度（第 02 章 `T.const`；部分旧版/低层代码也有
`T.dynamic`），`@jit` 从实际张量绑定；与 autotune 组合时通常要用
`set_autotune_inputs` 或按 shape bucket 提供具体形状。

## 8.10 工程化 checklist（交付前过一遍）

1. **基线**：先有正确内核 + `assert_allclose` 参考实现；
2. **空间**：tile 尺寸、`num_stages`、`threads` 纳入 configs，剔除超限配置
   （共享内存 > 上限、寄存器超标、线程数非法）；
3. **公平性**：`set_autotune_inputs` 固定输入；warmup 足量（GPU 频率/缓存预热）；
4. **校验**：容差要和 dtype、算子和参考实现匹配，不要把 `1e-2` 当成所有任务的默认答案；float8 注意 torch 版本；
5. **固化**：最优配置写回默认参数 + `save_to_disk` 归档；
6. **缓存**：确认 `TILELANG_CACHE_DIR` 在 CI/多机环境一致；
7. **报告**：记录 TFLOPS、与 vendor 库对比、运行环境（GPU/驱动/tilelang 版本）。
8. **调优成本**：记录候选数、失败/超时数、总 wall time、是否 early stop/分组编译；
9. **完整配置**：若候选含 `pass_configs`，与 tile 参数一起保存，不能只抄数值最快的一行。

## 8.11 本章小结

- 两个入口：`@jit`（推断型）与 `compile(out_idx=, target=)`（显式型）。
- autotune 三件套：`configs` 参数空间 + `set_autotune_inputs` 固定输入 + `ref_prog`
  校验；结果按签名缓存在 `~/.tilelang/cache`。
- 生产流程：基线 → 扫参 → 校验 → 固化 → 归档；性能一律用 TFLOPS + 对比报告。

## 8.12 Checkpoint

1. 给 config 生成器加入线程上限、shared memory 预算和 `BM/BN/BK` 合法性过滤；
2. 用同一批输入完成至少 2 个配置的正确性与 benchmark，解释为什么输入必须固定；
3. 删除 autotune 缓存后重新运行，再恢复缓存并确认命中行为；
4. 把最佳配置写入一份 JSON/报告，并记录「适用形状、GPU、版本和 fallback」。

## 口述自测（详答见第 10 章）

1. **autotune 的搜索空间会包含什么？为什么 tile 尺寸收益最大？**（BM/BN/BK/stages/threads；共享内存与寄存器约束下的折中）。
2. **如何保证调优公平？**（固定输入、warmup、同一次 do_bench 设置）。
3. **编译缓存命中的条件？**（版本、源码、free-vars、configs、args 全等）。
4. **动态形状 + autotune 为什么冲突？**（内置生成器要静态形状；需显式供数）。
5. **do_bench 与简单 time 的区别？**（CUDA event、warmup、多轮、排除启动开销）。
6. **多后端怎么部署？**（target 解析；构建期 CUDA/ROCm/Metal 开关）。
