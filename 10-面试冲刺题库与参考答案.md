# 第 10 章 面试冲刺：高频考点与参考答案

正文让你理解概念，口述则检验你能不能把技术判断说清楚。复习时常见的问题是：概念理解了、代码
也能跑，一到限时回答就说不出条件和证据。这一章把前 09 章的内容重新组织成问题与参考答案，
训练四件事：给出判断、解释原理、提供证据、说明边界。它不教你新的概念，也不鼓励你背固定话术。

用时先遮住答案，完整讲一遍，再对照自己漏掉的条件。答案只要涉及硬件峰值、指令、API 或
benchmark，就必须落到目标 GPU、软件版本和测量方法上；一句脱离条件的“更快”，没有技术含义。
本章侧重限时口述，第 11 章侧重书面推演与代码迁移。

## 10.1 考察范围与复习策略

面试前先摸清考察范围，再定复习重点。内核/推理优化岗位通常考四块，每块对应不同的题型和
回答方式：

1. **GPU 架构与并行编程基础**（warp、内存层次、同步、occupancy……）
2. **AI 算子优化与算法设计**（GEMM、FlashAttention、融合、量化）
3. **编程语言与编译**（会一门 DSL/编译框架的加分，TileLang/Triton/CUDA 皆可）
4. **工程与场景题**（benchmark 方法、性能报告、问题复盘）

针对这四块，给你三条策略：概念题用「结论 + 一句原理 + 一句『所以设计上会……』」的结构，
有观点才加分；现场编程题先讲思路再写代码，写完主动说清边界与复杂度；场景题套 10.6 的项目
模板，做到有数据、有对照、有复盘。三类题目的共同要求是一样的：完整说明你的技术判断。

## 10.2 GPU 架构与并行编程基础

这一节覆盖面试中的基础问题。单个问题通常不难，但面试官常会连续追问，看你是真懂还是只背
住了定义。请逐题口述，再对照答案补齐漏掉的依据和边界。

**Q1. GPU 为什么适合深度学习？和 CPU 的架构差异？**
可以从三个角度比较：

1. **硬件设计**
   - **CPU**：核心数量较少，但单个核心复杂。它使用大缓存、分支预测、乱序执行和较强
     的标量/向量执行能力，目标是尽快完成一条复杂或不规则的任务。
   - **GPU**：包含大量相对简单的执行单元，以线程块和 warp 为单位并行调度。它把更多芯片
     面积用于 ALU、寄存器、共享内存和高带宽内存接口，目标是同时处理大量相似任务。
     现代 AI GPU 还加入 Tensor Core 一类的矩阵乘加单元，用低精度高吞吐执行 GEMM、卷积
     等运算。
   - **TPU**：属于面向张量运算的专用加速器，典型结构包含矩阵乘单元或脉动阵列
     （systolic array）。它牺牲一部分通用性，换取规则矩阵计算的面积和能效优势。
   - **NPU**：通常指 SoC 或边缘设备中的神经网络加速器。它往往针对 INT8、FP16 等精度、
     常用算子和片上 SRAM 做优化，重点是低功耗推理。不同厂商的 NPU 在算子覆盖、数据布局
     和编译工具链上差异较大。

2. **编程模型**
   - **CPU** 主要使用进程、线程和 SIMD 指令。代码可以包含复杂分支、指针操作和不规则
     访存，开发灵活，线程之间的协作成本相对容易控制。
   - **GPU** 使用 kernel、grid、block 和 warp 的层次化模型。一个 warp 中的线程通常锁步
     执行同一条指令（SIMT）；如果线程走不同分支，就会发生分支发散并依次执行不同路径。
     程序员还需要关注 global memory、shared memory、寄存器、布局、同步和流水线。
   - **TPU** 更依赖图编译和算子编排，例如 XLA 的 SPMD 切分、张量布局和设备间分片。程序
     适合表达规则的计算图，运行时由编译器把大张量映射到矩阵单元和片上存储。
   - **NPU** 通常通过厂商 SDK 把模型转换成设备可执行的算子图。开发重点是算子支持、量化、
     内存布局和图编译；遇到不支持的算子时，可能需要回退到 CPU 或 GPU。

3. **使用场景**

   | 架构 | 更适合的任务 | 主要取舍 |
   | --- | --- | --- |
   | CPU | 控制流、数据预处理、小批量任务、分支多或访存不规则的算子 | 通用性和低延迟好，但大规模矩阵吞吐有限 |
   | GPU | 深度学习训练、通用推理、自定义算子和高吞吐批处理 | 软件生态成熟、灵活性高，但功耗、显存和 kernel 调度成本需要管理 |
   | TPU | 云端大规模训练和推理，以及规则的密集张量计算 | 矩阵计算效率高，但通常依赖特定平台、编译器和算子集合 |
   | NPU | 手机、PC、摄像头和边缘设备上的本地推理 | 能效和隐私较好，但模型兼容性、精度和工具链受厂商限制 |

4. **面试收尾**

   GPU、TPU 和 NPU 都可能加速深度学习，但「谁更快」不能脱离模型结构、输入形状、批量、
   精度、内存带宽、软件栈和功耗约束。GPU 的优势是通用性与生态之间的平衡；TPU 更偏向
   数据中心的规则张量吞吐；NPU 更偏向端侧的能效和集成。实际选型应先判断算子是
   memory-bound 还是 compute-bound，再用目标设备上的正确性测试和 benchmark 验证。

**Q2. 什么是 warp？什么是 warp divergence？**
答：CUDA 中 32 个线程组成一个 warp，是调度与执行的原子单位。同一 warp 内各线程
不能独立走不同分支——`if (tid % 2)` 会让 warp 先执行 then 再执行 else（或反过来），
浪费一半吞吐。**避免方式**：按 warp 对齐组织数据、用 predication、避免以线程奇偶
为分支条件。

**Q3. 说说 GPU 内存层次与各自延迟/容量。**
答：从离计算单元的距离、容量和访问速度看，GPU 常见的存储层次包括：寄存器、SM 上的
共享内存和 L1 Cache、GPU 级别的 L2 Cache，以及全局内存（global/device memory）。寄存器
通常最快但容量最小；共享内存和 L1 位于 SM 附近；L2 由多个 SM 共享；全局内存容量较大
但访问延迟通常更高。具体容量、延迟和 bank 模式随 GPU 架构变化，性能报告必须引用目标
GPU 的规格和实测指标。

这些层次不是所有访问都会依次经过的固定数据通路。共享内存是程序显式管理的 scratchpad；
L1 和 L2 主要由硬件管理；全局内存由程序分配和访问，但实际访问可能命中缓存。现代 NVIDIA
GPU 中，L1 Cache 和共享内存可能共享同一块物理资源，但二者的编程语义不同。全局内存的
物理实现可能是 HBM 或 GDDR，不能把 HBM 当成通用的 CUDA 内存层次。寄存器分配主要由
编译器决定。**就近放数据 + 复用**是优化的总纲。

**Q4. 什么是 bank conflict？**
答：在常见的 32-bit bank 模型中，共享内存有 32 个 bank。warp 线程同时访问地址
`a_i` 时，如果多个地址映射到同一 bank，可能产生冲突；「n 路冲突约慢 n 倍」只是
简化直觉，实际代价还受广播、访问宽度和架构影响。
**解决**：padding（行末加几字节）、换存储布局、swizzle（XOR 掩码打散）。TileLang
里就是 `annotate_layout + make_mma_swizzle_layout`。

**Q5. 什么是合并访问（coalesced access）？**
答：warp 内线程访问的地址越连续、对齐且集中，通常需要的 memory transactions 越少，
带宽利用率越高。不能脱离目标 GPU、访问宽度、对齐方式和活跃线程，简单断言「一定只需
一次事务」。常见做法是让相邻线程访问最内层连续元素。

**Q6. 什么是 occupancy？怎么算？**
答：occupancy = 活跃 warp 数 / 单 SM 最大 warp 数。由寄存器数、共享内存、block
大小共同限制：`min(寄存器上限/每线程寄存器, 共享内存上限/每块共享内存, 硬件线程上限)`
。高 occupancy 有利于藏延迟，但**不是越高越好**——寄存器降低导致局部性差时，低
occupancy + 高 ILP（指令级并行）可能更快。

**Q7. 访存延迟怎么隐藏？**
答：三层：① 高 occupancy（更多 warp 轮流跑，一个等内存时别的算）；② 指令级并行
（多独立访存同时发出）；③ 异步拷贝/流水线（cp.async/TMA 把搬运交给硬件，计算
重叠）。TileLang 的 `T.Pipelined` 就是第 ③ 层的自动版。

**Q8. 什么是 roofline 模型？怎么用它判断优化方向？**
答：把程序画在以「计算强度 = FLOPs/字节」为横轴、性能为纵轴的图上：斜率为带宽、
平台为峰值算力。程序点落在斜线上 = **memory-bound**（优先减访存：复用、融合、
低精度）；落在平台上 = **compute-bound**（优先减计算或提高指令效率）。**面试回答
一切优化问题先做这个判断**。

**Q9. Tail effect / wave quantization 是什么？**
答：GPU 以「wave」（一批可并行的 block）为单位调度。若总 block 数不是 wave 大小的
整数倍，最后一波不满载 → 尾波效应，利用率下降。可尝试调整 tile 尺寸，或用
**persistent kernel / Stream-K** 改变任务分配；它们可能缓解尾波，但也会引入循环取任务、
归并或并发度方面的代价。

**Q10. Tensor Core 是什么？mma/wmma/wgmma 区别？**
答：Tensor Core 是 NVIDIA 的矩阵乘加速单元。`wmma`、`mma`、`wgmma` 的可用性、
形状和指令路径取决于 compute capability、dtype 和编译器后端；不要在没有目标架构
信息时背固定版本映射。**接近峰值性能的条件**：数据布局匹配硬件片段 + 流水线充分隐藏延迟 +
寄存器压力管理，并用生成代码和 profiler 证明。

**Q11. 什么是 TMA？Hopper 引入它解决了什么？**
答：在支持的 NVIDIA 架构和工具链上，TMA 可把多维 global→shared/其他目标搬运交给专用路径，
配合 descriptor 和 mbarrier 做完成信号。它的收益与限制取决于架构、形状、布局和
TileLang 版本；面试应同时说清「需要目标支持」和 fallback，而不是把 TMA 当成所有 GPU 的默认路径。

**Q12. 什么是 warp specialization / 生产者-消费者 warp？**
答：把 warp 分工：一部分专做搬运（生产者），一部分专做计算（消费者），通过
mbarrier/共享内存交接，避免「同一个 warp 又要搬又要算」带来的停顿。TileLang 的
例子（`examples/warp_specialize/`）可能与 persistent + TMA 组合使用，但是否适合要由
目标架构、问题形状和 profiler 结果决定。

## 10.3 TileLang 语言与编译

基础概念之后换到 TileLang 这个具体载体。这部分考的是：你不仅会写内核，还知道
`@T.prim_func`、`T.Kernel`、fragment 布局这些抽象背后发生了什么——一句话，你写出来的
代码只是水面上的那部分。逐题回答时，尽量用第 09 章的编译流水把答案串起来。

**Q13. TileLang 是什么？与 Triton、CUDA、TVM 的关系？**
答：TileLang 是基于 TVM TIR 的 tile 级内核 DSL。它允许显式放置 shared/fragment、
控制流水线和布局，也能在需要时下探到 warp、PTX、TMA 等目标相关路径。CUDA/CuTe
提供更底层的控制，但实现和维护成本通常更高；Triton 通常让编译器承担更多布局和
shared-memory 决策，简单 elementwise/fusion 往往更快写出。实际差异随版本和算子变化，
选型证据应来自同一目标算子的开发成本、正确性覆盖、目标 shape 性能和生成代码。

**Q14. `@T.prim_func` 干了什么？**
答：把 Python 函数体**解析成 TIR**（TVM IR），不是执行。函数体内的「运算」变成 TIR
表达式节点，随后进入编译流水（第 09 章）。推论：不能用 Python 任意库，条件/循环
由 TileLang 解释。

**Q15. `T.Kernel(...)` 与 CUDA 的对应？thread 去哪了？**
答：`T.Kernel(gx, gy, gz, threads=t)` 声明 grid 与每块线程数，`as (bx,by,bz)` 是
`blockIdx`。**线程索引不需要手写**：`T.Parallel` 循环由编译器映射到线程并向量化；
需要原始线程号时可用 `T.get_lane_idx()` 等原语（L3 场景）。

**Q16. fragment 布局推断到底解决什么问题？**
答：fragment 是「伪形状」（如 128×128），真实情况是每个线程只持有其中一部分。
布局推断 = 编译器根据所有操作（MMA 片段要求、copy 的访存模式、Parallel 循环）求解
一致的每线程分配，让你写 tile 级代码。代价：约束冲突/布局不佳时需
`T.annotate_layout` 显式指定布局（如 MMA 所需的 swizzle 布局）。

**Q17. `T.copy` 是同步还是异步？**
答：对外**同步语义**——语句完成后 dst 可安全消费。内部可降级为
`ld.global / ldmatrix / cp.async`（自动补 commit/wait）/TMA，语义不变。要显式异步
用 `T.async_copy` 的显式异步路径；在 CUDA 上通常需要在消费前使用相应 wait group，
这是写手工流水线的 L3 手段，具体 API 以目标版本为准。

**Q18. `T.Pipelined(num_stages=3)` 的原理？**
答：把循环体拆成生产者（拷 tile→shared）与消费者（计算），做多级缓冲（double/
triple buffering），拷贝提前预取、与计算重叠：prologue 预取前几级，steady-state
每轮「拷 k+1 + 算 k」，epilogue 收尾。成本：共享内存 ×stages。手工不顺时可用
`stage/order` 注解（只标副作用语句；标量 Bind 不占槽位）。

**Q19. swizzle 的两种含义？**
答：① 共享内存**布局 swizzle**：重排物理映射以减少特定访问模式下的 bank conflict（
`make_mma_swizzle_layout`）；② **光栅化 swizzle**（`T.use_swizzle(panel_size=...)`）：
改变 block 调度顺序（对角线化）提升 L2 局部性。两者都叫 swizzle，一个管内存内
布局，另一个控制块间调度顺序。这是面试中常见的混淆点。

**Q20. `@tilelang.jit` 与 `tilelang.compile` 的区别？**
答：jit 是工厂函数装饰器，首次调用从**实际张量**推断 target 与形状、编译并缓存；
compile 是显式入口：`compile(func, out_idx=[...], target="cuda")`，`out_idx` 声明
输出参数——调用时自动分配并返回输出张量。执行后端 `tvm_ffi/cython/nvrtc/torch`
一般 auto。

**Q21. autotune 怎么保证最终配置可信？**
答：同一输入（`set_autotune_inputs` 固定、或 supply_type 生成器）→ 每个配置并行
编译 → `ref_prog`/容差校验正确性 → `do_bench`（warmup+rep）→ 取延迟最优 → 结果
按（版本+源码+configs+args）缓存磁盘。可信性来自：公平输入、足量 warmup、正确性
把关、可复现缓存。

**Q22. 动态形状怎么处理？**
答：当前 eager/JIT 主线可用 `T.const("M, N, K")` 让输入张量形状在调用时绑定；部分
旧版/低层代码也会出现 `T.dynamic`。注意：autotune 内置供数能力受形状和版本限制，
动态形状通常应使用 `set_autotune_inputs` 或按桶（bucket）预编译。

**Q23. 说说 TileLang 的编译流水？**
答：prim_func → TIR → 渐进式 lowering：`LowerTileOP`（展开 tile 算子）、
`LegalizeSafeMemoryAccess`（越界保护）、`LayoutInference`（布局求解）、
`InjectSoftwarePipeline`（流水线化）、`ThreadSync`（插屏障）→ codegen 出
CUDA/HIP → nvcc/cubin。（详见第 09 章，能画出这条链是深度加分项。）

**Q24. TileLang 相比 Triton 的优势与劣势？（开放题）**
答：优势：三级抽象可下探 L3（PTX、warp 原语、TMA）、布局显式可控、与 TVM 生态
（BitBLAS）联动；是否贴近库性能要看算子、硬件、版本和实测；劣势：社区/工具
链规模小于 Triton、文档与 API 迭代快、门槛高。面试说「我两者都了解，选型看需求」
最稳。

## 10.4 算子优化与算法设计

这是本章的重点：你能不能完整说出 GEMM、FlashAttention 这些算子的设计、瓶颈、证据和边界。
下面这些题要求你脱稿讲出完整设计思路，并回答追问。

**Q25. 口述一个高性能 GEMM 的完整设计思路（最核心大题）。**
答（四段式）：

1. **bound 判断**：先估算算术强度，再用 profiler 验证是 memory-bound 还是
   compute-bound；常用 FP16 大矩阵可能更接近计算受限，但不能预先假定；
2. **数据流**：每个 block 负责 BM×BN 输出 tile；K 维按 BK 切块；global→shared
   （`T.copy`，可异步）→ fragment 累加器（`T.gemm` → Tensor Core）→ epilogue 写回；
3. **流水线**：`T.Pipelined(num_stages=2~4)` 让拷贝与 MMA 重叠；共享内存上限决定
   stages 与 tile 的组合；
4. **细节**：用访问/布局证据判断是否需要 swizzle；用实测判断 rasterization 是否
   改善 L2；autotune 扫 BM/BN/BK/stages/threads；非整除尺寸显式设计 padding/guard。

收尾：**「最终用 do_bench 实测 TFLOPS，与 cuBLAS 对比并归因差距」**，把观点落到
可复现的证据上。

加分追问三连（考察是否真做过，而不只是背过）：

- **精度语义**：fp32 输入走 `T.gemm` 在 SM80 上可能静默降为 TF32（尾数 10 位），
  严格 `1e-5` 检查会挂——说清「实际执行精度」和验证容差才算答全。
- **大形状归因**：16384² 这类规模，慢的原因常常不在 tile 而在**调优空间缺维度**
  （L2-swizzle/num_warps/num_stages）与 **L2 驻留**（工作集远大于 L2）；能说出
  「扩 autotune 空间 + 单独开 `T.use_swizzle` 看 L2 命中」就是深度。
- **库对比口径**：相对效率（`latency_ref / latency_tl > 1` 才叫快）与 roofline 参照指标
  `ρ = W/(t·P)` 两个一起报，避免「比慢基线快」的假赢。

**Q26. FlashAttention 为什么快？在线 softmax 推导？**
答：朴素 attention 的 S/P（N×N）要落盘两次，带来 O(N²) 中间张量的 HBM 往返；是否
memory-bound 要由输入和 profiler 判断。FA
分块 + 在线 softmax：维护行统计 (m, l)，每来一块：
`m_new=max(m_prev,m_local)`；`l_new = l_prev·e^(m_prev−m_new) + l_local·e^(m_local−m_new)`；
`O_old *= e^(m_prev−m_new)`。**旧输出乘以修正因子即可，无需重读**。IO 复杂度降到
避免完整 S/P 落盘；更精确的复杂度取决于片上容量、tile 形状和实现，常见分析可写成
`O(N²·d²/M)` 加上线性项。反向靠重计算 + 存 lse。
（推导要能脱稿写，第 06 章有完整代码。）

**Q27. softmax 的数值稳定处理？**
答：`softmax(x_i) = exp(x_i − m)/Σexp(x_j − m)`，先减行 max 再 exp 防溢出；
TileLang 实现中更精细：`exp2(x·scale)` 把 `/√d` 与 `exp` 融合（scale 含 log2e），
少一次除法还更稳。FA 里这变成「在线 max 滚动更新」。

**Q28. 为什么做算子融合？举例。**
答：减少**中间张量落盘/回读**（IO 是最大成本）。例：GEMM+ReLU 融合（省写+读 C）、
Softmax 融合进 attention（省 P 落盘）、layernorm+residual+量化 融合到 epilogue。
TileLang 里就是 epilogue 直接在 fragment 上算（`acc_o /= logsum` 那样的写法），
零额外访存。

**Q29. 低精度（FP8/INT4）GEMM 的关键点？**
答：① 精度：scale 分组（per-tensor/per-channel/per-block）、fp8 e4m3（前向）vs
e5m2（梯度）选择、累加仍 fp32；② 硬件：支持 FP8 Tensor Core 的 GPU 可能获得更高吞吐，
但具体峰值和实际收益要查目标型号规格并实测；③ 布局：
权重重排（如 int4 打包、2:4 稀疏元数据）；④ 反量化融合进 epilogue。TileLang 有
`gemm_fp8 / gemm_int4` 官方示例与 `T.gemm_sp`（2:4 稀疏）。

**Q30. Split-K / persistent / Stream-K 分别解决什么？**
答：Split-K：K 太长时把同一输出 tile 的 K 拆给多个 block，最后原子/二次归并——用
并行换关键路径；persistent：固定少量 block 循环取 tile，消除启动/尾波开销；
Stream-K：把每个输出 tile 的工作量切成固定大小任务分给固定 block，负载均衡最优
（可能减少 tail effect；是否需要原子或额外归并取决于具体任务划分）。面试结论：**大矩阵
GEMM 三选一或组合，取决于 size、硬件和归并代价**。

**Q31. decode（Token 生成）场景怎么优化 attention？**
答：decode 是 memory-bound（每 token 读全部 KV）：① **GQA/MQA** 多 query 共享
KV 缓存减带宽；② **PagedAttention** 块状 KV 缓存减碎片/增并行；③
**FlashDecoding**：把 K/V 行分给多个 block 并行算部分 attention，用 lse 合并
（仍是 online softmax 思想）；④ MLA（DeepSeek）：KV 压缩成低秩 latent，decode
带宽大幅下降（官方有 `examples/deepseek_mla/`）。

**Q32. 怎么证明你的优化有效？（benchmark 方法论）**
答：① 正确性先行：与参考实现 assert_allclose（容差明确，且容差按 dtype 定：fp32 1e-5、
fp16 1e-3、bf16 1e-2 量级，归约放宽）；② 变量控制：同一输入、同一 GPU/驱动、同
warmup/rep，必要时锁时钟（locked clock）把 run-to-run 波动压到 1% 以内；③ 指标：延迟
+ 派生指标（TFLOPS/带宽利用率），**与硬件理论峰值对照**，再算 roofline 参照指标
`ρ = W/(t·P)` 作为与基线无关的第二信号；④ 对照：与 cuBLAS/官方库、与优化前版本；
⑤ 归因：每一步优化单独开关，量化单项收益（如 swizzle 开/关）。面试加分：报告里带
「内存墙/计算墙判断 + 差距归因」，并能说清「cliff 大但 ρ 仍低 = 结构性上限」这类区分。

**Q33. 如果问你「共享内存不够了怎么办」？（开放）**
答：① 缩小 tile 或 stages（autotune 空间里约束共享内存）；② 减少缓冲副本（能
fragment 直算就不经 shared）；③ 数据复用策略（A 读一次多算几个输出块）；④ 用
L2 语义优化（光栅化调度）减轻对 shared 的依赖；⑤ 换更省内存的算法结构（如
Stream-K 任务化）；⑥ fp8/换 dtype 减半占用。逐条评估并讲取舍即可。

## 10.5 现场编程（面试模板 + 评分点）

概念和设计最终要落到代码上。现场编程题不要求你背代码，考的是思路有没有条理：请先用
30 秒讲思路，再动笔写，写完主动说清复杂度与边界。最怕一坐下就埋头写字，写完才发现结构
是错的。

> 现场编程题通常按以下顺序评分：正确性 → 并行化 → 访存 → 流水线 → 边界。先用 30 秒讲思路，
> 再写；写完主动说复杂度与边界处理。

**模板 1：vector add（热身）**

```python
# 输入 A/B/C 都是一维 tensor，shape=[N]；每个 block 处理 256 个元素。
@T.prim_func
def add(A: T.Tensor((N,), 'float32'), B: T.Tensor((N,), 'float32'),
        C: T.Tensor((N,), 'float32')):
    # bx 是 block 索引，不是线程索引；Parallel 循环负责线程映射。
    with T.Kernel(T.ceildiv(N, 256), threads=256) as bx:
        for i in T.Parallel(256):
            # 显式保护尾部元素，避免 gi>=N 时访问越界。
            gi = bx * 256 + i
            if gi < N:
                C[gi] = A[gi] + B[gi]
```
口头补充：先用测量判断 memory/compute bound；`T.Parallel` 提供并行迭代空间；边界
guard 仍需在语义上明确。

**模板 2：tiled GEMM（先理解，再默写；下面假设尺寸整除 tile）**

```python
# A=[M,K]、B=[K,N]、C=[M,N]；下面假设 M/N/K 都整除 tile。
@T.prim_func
def gemm(A: T.Tensor((M, K), 'float16'), B: T.Tensor((K, N), 'float16'),
         C: T.Tensor((M, N), 'float16')):
    # 每个 block 计算 C 的一个 BM×BN tile。
    with T.Kernel(T.ceildiv(N, BN), T.ceildiv(M, BM), threads=128) as (bx, by):
        # A_s=[BM,BK]、B_s=[BK,BN]、C_f=[BM,BN]。
        A_s = T.alloc_shared((BM, BK), 'float16')
        B_s = T.alloc_shared((BK, BN), 'float16')
        C_f = T.alloc_fragment((BM, BN), 'float32')
        T.clear(C_f)
        for ko in T.Pipelined(T.ceildiv(K, BK), num_stages=3):
            # 每轮搬运 A/B 的 K tile，再累加到 fp32 fragment。
            T.copy(A[by * BM, ko * BK], A_s)
            T.copy(B[ko * BK, bx * BN], B_s)
            T.gemm(A_s, B_s, C_f)
        T.copy(C_f, C[by * BM, bx * BN])
```

**模板 3：FA 前向骨架**（见第 06 章完整代码，背 6.3 的 90 行）

**模板 4：如果面试官要 CUDA 伪代码版 GEMM**
答：gird = (N/BN, M/BM)；每 block：`__shared__ A_s[BM][BK], B_s[BK][BN]`；
K 循环 { 协作拷贝（tid 映射元素）+ `__syncthreads` + 寄存器累加 + `__syncthreads` }；
写回。再补三句优化：double buffering、swizzle、ldmatrix。**说得出完整结构 + 优化
点就够了**，不用真写 200 行。

**补充说明示例**：「这个 kernel 是 compute-bound（算一下访存/计算比）…… 所以我
用 Tensor Core + 流水线；如果 K 很大我会考虑 split-K，如果 M/N 不是整数倍我会
padding 或 predicate。」

## 10.6 项目复盘模板与场景题

一道完整的场景题，往往从"讲讲你做过的一个算子"开始。这里的模板帮你在几分钟内把一个
项目讲完整：背景、指标、演进、数据和复盘结论。少了其中任何一项，都容易被追问到卡住。

**讲项目八股（STAR 变体 + 技术深度）**：
1. 背景与问题（什么算子、什么场景、为什么自研/用 TileLang）；
2. 目标与指标（延迟/吞吐/TFLOPS，对齐到哪个峰值）；
3. 方案与演进（v0 朴素 → 分块 → 流水线 → 布局/光栅化 → autotune，每步附收益）；
4. 数据与验证（表格/图：与 cuBLAS、Triton、优化前对比）；
5. 复盘与下一步（瓶颈在哪、如果换硬件怎么做）。

**高频追问与应对**：

- 「为什么不用 cuBLAS？」 → 融合需求（epilogue 定制）、算子新（FA 变体）、学习/自研
  目标、cuBLAS 不可控。
- 「调参空间多大？最影响性能的是哪个？」 → tile 尺寸 > stages > threads；共享内存
  与寄存器约束下 trade-off。
- 「结果比官方慢怎么办？」 → 归因流程：bound 判断 → 生成源码审访存/流水 → Nsight
  Compute 看 stall → 单点优化验证（这正是第 09 章决策树）。
- 「内存搬运占比怎么算？」 → 理想访存 = 3·M·N·dtype + K 维复用项；实测带宽/理论带宽。

## 10.7 常用答题结构

前面的题目可以归纳为几种常见结构。熟悉下面三种组织方式，限时回答时就不容易讲漏。

**优化题常用结构**：判断 bound → 画数据流 → 定并行结构 → 使用流水线 → 检查实现细节 →
数据验证。
**概念题常用结构**：定义 → 原理（一句话）→ 对设计的影响（所以……）→ 例子。
**开放题常用结构**：先给出结构（「我会分三步……」）→ 每步给理由 → 最后说明
验证手段。回答优化题时先提出 bound 假设，再说明怎样测量；不要在没有数据时直接断定
它一定是 memory-bound 或 compute-bound。

## 10.8 冲刺自测清单（考前 24h 逐项打勾）

考试前一天，用这张清单做最后一遍核对。每打一个勾，说明那项已经能在不看笔记的情况下讲出
来，而不是只剩模糊印象。能不能说清条件和依据，直接决定后续追问会把你带到哪。

- [ ] 能默写：GEMM 模板、FA 前三式推导、在线 softmax 三公式
- [ ] 能口述：内存层次表、bank conflict 画图、roofline 图
- [ ] 能对比：TileLang vs Triton vs CUDA；T.copy vs async_copy；swizzle 双义
- [ ] 能答：为什么 fp32 累加、为什么减 max、为什么 TMA 省寄存器
- [ ] 有项目案例：数据流 + 收益数字 + 一次问题复盘
- [ ] 过一遍第 12 章速查表 + 第 11 章模拟题

## 10.9 本章回顾

这一章把九章的实践整理成可以复述的表达。面试的分量大致是：架构基础（50%）+ 算子
设计（30%）+ 工程复盘（20%），TileLang 是贯穿主线、把知识串起来的载体。落到答题动作上，
一切优化题先判断 bound，一切结果题给出「数据 + 对照 + 归因」，GEMM/FA 模板该练到能独立
写出并解释。面试官先看思路成不成立，而不是代码写得整不整齐。这一章练的，就是把你已经掌握
的内容说准，并让回答经得起追问。

## 10.10 自测门槛

随机抽 5 题，每题限时 90 秒，用下面四条标准判断自己是不是真过了这一章。判定的重点不是
"答没答对定义"，而是"有没有给出条件和证据"——没有条件的答案，在面试里等于没有答案。

- 只说定义，不说适用条件：不通过；
- 只说「更快」，没有输入/硬件/计时方法：不通过；
- 能把实现、测量、瓶颈和 trade-off 连成一条链：通过；
- 能主动指出版本/架构 caveat，并给出 fallback：优秀。
