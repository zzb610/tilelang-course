# 第 02 章 核心语法：prim_func、Kernel、循环

第 01 章我们跑通了第一条闭环，也反复看到一个词：内核。可你或许已经隐约感觉到，
那个 vector add 里藏着不少还没讲透的东西——`@T.prim_func` 到底是什么，`with T.Kernel(...)`
里的参数为什么那样写，循环为什么有的写 `T.Parallel` 有的写 `T.serial`。这一章就来回答
这些问题：建立一个 TileLang 的最小语法模型，先声明张量，再配置 block，随后用循环、
作用域和控制流表达计算。学完这一章，你应当能读懂一个简单内核，并亲手写出带边界处理的
elementwise 基线和一个刻意串行的归约基线。有了这个底座，后面关于内存、流水线和矩阵乘
的每一章，都是在它的地基上添砖。

> **本章导航** 入门难度，预计 3–4 小时；前置是第 01 章，且要能区分编译期参数和运行时张量。
> elementwise 示例可在更多后端尝试，归约/布局示例以 CUDA 为主。学完你会留下一个 2D
> elementwise 内核、一个「故意串行」的归约骨架，以及一份循环/作用域速查表。写作参考：
> [Language Basics](https://tilelang.com/programming_guides/language_basics.html)、
> [Control Flow](https://tilelang.com/programming_guides/control_flow.html)、
> [Type System](https://tilelang.com/programming_guides/type_system.html) 和
> [Python Compatibility](https://tilelang.com/programming_guides/python_compatibility.html)。

本章建议按「形状与类型 → 启动配置 → 循环语义 → 内存作用域 → 数据搬运与控制流 →
完整小例子 → 调试」的顺序阅读：前半部分先解决「代码怎么读」，后半部分再解决「代码怎么写」，
而不要在还没分清 block 索引和元素索引之前，就直接跳到 GEMM 模板。

## 2.1 内核入口：`@T.prim_func`

要理解 TileLang，先要接受一个听起来反直觉的事实：内核里的那些 Python 代码，并不会被
Python 逐行执行。TileLang 内核就是一个被 `@T.prim_func` 装饰的 Python 函数，装饰器会把
函数体**翻译成 TIR**（TVM IR），而不是执行它。由此引出几个重要推论：

- 函数体里的「变量」多是 TIR 表达式（符号计算），不是 Python 对象；
- 支持 Python 的 `if/else`、`for` 文法，但语义由 TileLang 解释；
- **不能用 Python 的任意库函数**（如 `numpy`），只能用 `T.*` 提供的运算与
  TVM TIR 内建数学函数（`T.exp`、`T.max`、`T.rsqrt` …）。

读一段内核时，与其纠结某一行「是什么」，不如先把每个量放回它的生效阶段。下面这张表
帮你建立这个习惯：

| 阶段 | 典型内容 | 你要问的问题 |
|---|---|---|
| Python/编译期 | `M`、`N`、`block`、`dtype` | 这些参数是否会改变生成的内核？ |
| TIR 构造期 | `T.Tensor`、`T.Kernel`、`T.Parallel` | 计算和数据访问如何被表达？ |
| 设备运行期 | `A[i]`、`C[i]`、线程执行 | 哪些线程访问哪些元素？边界是否安全？ |

### 2.1.0 数据流算子与调度原语：本课程的总纲

在进入具体语法之前，先立起贯穿全书的一条划分。TileLang 论文把语言要素分成两列，读任何
一段内核前先判断它属于哪一列：

| 类别 | 作用 | 典型 API | 谁来决定实现 |
|---|---|---|---|
| **数据流算子**（dataflow operators） | 表达「算什么、数据怎么流」 | `T.copy`、`T.gemm`、`T.reduce`、`T.atomic`、`T.Parallel` | 编译器选择线程绑定、向量化、指令 |
| **调度原语**（scheduling primitives） | 接管编译器默认调度中的某一维 | `T.Pipelined`、`T.annotate_layout`、`T.use_swizzle`、`T.ptx` | 用户显式指定，编译器负责其余部分 |

这两列的分工还有一条更底层的机制作支撑：每个数据流算子都实现了两个接口——**Lower**
（如何降级成线程绑定、向量化访存等低层 IR）和 **InferLayout**（如何推导 buffer 与循环的
布局）。这正是 `T.copy(A, A_shared)` 一句能自动生成合并、向量化的拷贝循环，而 `T.gemm`
会同时决定 shared 输入的 swizzle 布局与 MMA 输出 fragment 布局的原因：算子把「数据流」
留给你，「调度」自己算，但你随时可以插手。

由此也能看清 TileLang 与 Triton 的分工差异：Triton 把 shared memory 分配、布局和流水线
大多藏在编译器里，只留 `num_stages` 等少量旋钮；TileLang 则让你显式声明 buffer 放在哪
一级、流水线怎么排、布局是什么，同时用布局推断免去逐线程的手工管理。第 03～07 章讲的
正是「先信任推断、再用证据接管」的完整过程。

### 2.1.1 参数声明：`T.Tensor` / `T.Buffer`

```python
@T.prim_func
def add_kernel(
    A: T.Tensor((N,), dtype),       # 一维
    B: T.Tensor((M, K), dtype),     # 二维
    C: T.Tensor((B, S, H, D), dtype),  # 四维（Transformer 的 bshd 布局）
):
    ...
```

关于形状，有两点值得记住：形状可以是具体整数，也可以是符号（见 2.2）；而 `T.Buffer`
与 `T.Tensor` 都是缓冲声明，教程中两者通用。

### 2.1.2 dtype 的三种写法（选一种并保持一致）

```python
dtype = 'float32'        # 字符串
dtype = T.float32        # TileLang dtype 对象
dtype = torch.float32    # 框架 dtype，直接可用
```

这三种写法等价，真正需要注意的是选一种并在整段代码中保持一致。常用类型包括：
`float16`、`bfloat16`、`float32`、`float64`、`int8/16/32/64`、`uint8/…/uint64`，
以及低精度族 `float8_e4m3fn`、`float8_e5m2` 等（依赖后端支持）。

### 2.1.3 混合精度是常态

在 GPU 内核里，「输入一种精度、累加另一种精度」是常态而非例外。GEMM 的典型做法是
`fp16` 输入、`fp32` 累加：

```python
C_f = T.alloc_fragment((BM, BN), 'float32')   # 累加器用 fp32
```

本章只需先记住一个要点：累加器通常用 fp32，一是为了减少误差累积，二也常是为了匹配
Tensor Core 的累加精度要求。

## 2.2 动态（符号）形状

形状不必写死。当前官方 eager/JIT 写法优先使用 `T.const` 在 `@jit` 函数中声明符号维度：

```python
# @jit 函数直接接收张量；实际调用时由输入形状绑定 M/N
@jit
def add_dynamic(A, B):
    M, N = T.const("M, N")
    dtype = T.float32
    A: T.Tensor((M, N), dtype)
    B: T.Tensor((M, N), dtype)
    C = T.empty((M, N), dtype)

    with T.Kernel(T.ceildiv(N, 64), T.ceildiv(M, 64), threads=128) as (bx, by):
        for i, j in T.Parallel(64, 64):
            gi, gj = by * 64 + i, bx * 64 + j
            if T.all_of(gi < M, gj < N):
                C[gi, gj] = A[gi, gj] + B[gi, gj]
    return C
```

在 `@jit` 外层工厂函数下，具体尺寸在**首次用实际张量调用 kernel 时**绑定，因此调用者
无需手写 `M/N`。动态形状与 autotune 的配合见第 08 章。

符号维度的 API 存在版本差异，跨版本复制代码前先在本机确认：

| API | 状态（以本机 TileLang 0.1.13 实测为准） | 说明 |
|---|---|---|
| `M, N = T.const("M, N")` | 当前 eager/JIT 主线写法 | 本教程正文使用 |
| `T.dynamic("K")` | 可用 | term-level 符号变量，文档站同时推荐 |
| `T.dyn['K']` | 文档站推荐用于类型注解 | 本机 0.1.13 尚无此入口，属更新版本 |
| `T.symbolic("K")` | 已弃用（v0.1.9 起） | 旧代码别名，看到时改写为 `T.dynamic` |

值得记下一个小技巧：在 kernel 体内，还可以直接从参数 buffer 读回绑定后的尺寸（如
`M = A.shape[0]`），避免手工传一遍符号。反过来，若某段示例在本机报 `AttributeError`，
优先检查是否用了另一个版本的符号 API，而不是怀疑内核逻辑。

## 2.3 启动配置：`T.Kernel`

声明了张量和计算之后，还要告诉编译器「这次启动多少 block、每 block 多少线程」，
`T.Kernel` 干的就是这件事。

```python
# 用三个符号网格维度和一个线程数，声明 block 的启动配置。
with T.Kernel(grid_x, grid_y, grid_z, threads=T) as (bx, by, bz):
    # bx/by/bz 是 block 索引；线程映射由内部 Parallel 循环决定。
    ...
```

它的四个参数各有所指：前三个参数是网格（grid）各维大小，对应 `blockIdx.x/y/z`；
`threads=` 是每个 block 的线程数，CUDA 设备常见上限为 1,024，但仍应查询目标设备，
其它后端也不承诺相同限制。解包出来的 `bx/by/bz` 是块索引，**不是线程索引**——线程
索引/线程映射由编译器从 `T.Parallel` 等循环推断（第 03 章讲原理）。也可以省略维度只取
一两个：`as bx`、`as (bx, by)`。

```python
# 处理 (M, N) 矩阵的典型写法：每个 block 负责一块 tile
with T.Kernel(T.ceildiv(N, BN), T.ceildiv(M, BM), threads=threads) as (bx, by):
    ...
```

本章要点落在这一问上：**为什么用 `T.ceildiv(N, BN)` 而不用 `N // BN`？**——格子数量
必须向上取整，保证覆盖余数部分（边界由安全访问 pass 或显式判断处理）。

## 2.4 循环：四种核心结构

循环是内核的骨架，而不同循环结构对应不同的硬件行为。下面这张表先给全景：

| 结构 | 语义 | 硬件对应 | 典型场景 |
|---|---|---|---|
| `T.serial(n)` | 普通顺序循环 | for 循环 | 归约循环、沿 K 维的串行累积 |
| `T.unroll(n)` | 请求展开 | 展开后的直列代码 | 小常数循环，省分支 |
| `T.Parallel(a, b, ...)` | 嵌套并行循环 | 映射到线程 + 向量化 | elementwise、tile 内拷贝 |
| `T.Pipelined(n, num_stages=k)` | 软件流水线 | 异步拷贝重叠计算 | GEMM/Attention 的 K 维主循环 |

写法上的差别用几行就能看清：

```python
for i in T.serial(N):            # 单循环
    ...

for i, j in T.Parallel(M, N):    # 双循环：i 和 j 一起展开
    C[i, j] = A[i, j] + B[i, j]

for ko in T.Pipelined(T.ceildiv(K, BK), num_stages=3):
    T.copy(A[by*BM, ko*BK], A_s)
    T.copy(B[ko*BK, bx*BN], B_s)
    T.gemm(A_s, B_s, C_f)
```

此外还有更高级的控制：`T.Persistent(...)`（持久化线程块模板）、Python `while`、
`break/continue`。这些语句由 TileLang 转成 TIR 控制流，见第 04 章。

## 2.5 内存分配三兄弟

声明了计算和数据移动之后，就该考虑数据放哪了。TileLang 用三个 `alloc_*` 原语区分三种
作用域：

```python
A_shared = T.alloc_shared((BM, BK), 'float16')   # block 内共享，片上共享内存
C_frag   = T.alloc_fragment((BM, BN), 'float32') # 寄存器片段（每线程一份，布局由编译器推断）
s        = T.alloc_var('int32')                  # 标量寄存器变量
T.clear(C_frag)                                  # 清零累加器
T.fill(C_frag, 1.0)                              # 填充
```

三者分别落在不同的硬件层次和可见范围上：

| 分配 | 作用域 | 谁可见 | 说明 |
|---|---|---|---|
| `alloc_shared` | shared memory | 整个 block | 显式数据中转站，global→shared→compute |
| `alloc_fragment` | 寄存器 | 单线程 | 「伪 2D 形状」实际按布局切给各线程；适合累加器/局部结果 |
| `alloc_var` | 寄存器 | 单线程 | 标量变量，常用于归约或索引 |

本章要点是**理解 fragment 的「布局推断」**：你写 `(BM, BN)` 的 fragment，编译器会决定
每个线程持有其中哪些元素（如 MMA 的 16×8 片段），这是 TileLang 相对 CUDA 的核心抽象
之一，第 03、07 章展开。

## 2.6 数据搬运：`T.copy`

分配好 buffer 之后，数据要在不同作用域之间流动，`T.copy` 承担绝大多数这类工作。

```python
# 规则的矩形 tile：优先使用高层搬运原语
T.copy(A[by * BM, ko * BK], A_shared)

# 寄存器片段 → 全局（写回结果）
T.copy(C_frag, C[by * BM, bx * BN])

# 只有需要逐元素变换、掩码或转置时，才手写循环
for k, j in T.Parallel(BK, BN):
    B_shared[k, j] = B[ko * BK + k, bx * BN + j]
```

`T.copy` 表达的是「搬运一个区域」，范围通常由源或目标的形状推断；编译器再根据目标和
布局选择合适的实现。它对外保持同步语义：语句完成后才能按普通方式消费 `dst`。

单元素赋值与 `T.copy` 只有在标量场景下近似等价：

```python
B[i] = A[i]          # 一个元素
T.copy(A[i], B[i])   # 也可以表达一个元素
```

但要小心：`B_shared = A[... ]` 不能用一个 `=` 代替，因为它搬运的是一个 tile，需要
`T.copy` 或显式循环。于是用法就有了清晰的分界：对规则矩形搬运，优先使用 `T.copy`；
只有需要转置、掩码、补零或自定义索引时，才使用 `T.Parallel` 配合逐元素赋值。异步搬运
另看第 04、07 章，不要仅凭生成代码猜同步语义。

## 2.7 控制流与逻辑

除了循环，内核还需要条件分支和多条件组合。

```python
# 条件分支（Python 文法，TIR 语义）
if i < N:
    C[i] = A[i]
else:
    C[i] = 0

# 三目
x = A[i] if i < N else 0

# 多条件：用 T.all_of / T.any_of 表达更清晰
if T.all_of(gi < M, gj < N):
    C[gi, gj] = ...
```

这里有一个容易踩的细节：`if` 条件里如果有 Python 常量（如 `if True:`），会在编译期折叠。

## 2.8 数学函数与辅助

做计算时，常用的数学函数和辅助原语都在 `T.*` 命名空间下：

```python
T.exp(x); T.log(x); T.rsqrt(x); T.max(a, b); T.min(a, b)
T.exp2(x)          # 乘 2 的幂，配合 scale 融合（FA 里常用）
T.if_then_else(c, a, b)
T.ceildiv(m, n)    # 向上取整除法
T.infinity('float32')
```

## 2.9 完整示例一：2D elementwise（SAXPY 升级版）

前面的语法点各自独立，现在把它们拼成一个完整内核。下面这个 2D elementwise 是第 01 章
vector add 的二维升级版：

```python
import torch
import tilelang
import tilelang.language as T
from tilelang import jit

@jit
def elementwise_2d(M: int, N: int, block_m: int = 64, block_n: int = 64,
                   dtype: str = 'float32'):
    @T.prim_func
    def kern(
        A: T.Tensor((M, N), dtype),
        B: T.Tensor((M, N), dtype),
        C: T.Tensor((M, N), dtype),
    ):
        with T.Kernel(T.ceildiv(N, block_n), T.ceildiv(M, block_m),
                      threads=256) as (bx, by):
            # 每个 block 处理 block_m × block_n 的 tile
            for i, j in T.Parallel(block_m, block_n):
                gi, gj = by * block_m + i, bx * block_n + j
                if T.all_of(gi < M, gj < N):
                    C[gi, gj] = A[gi, gj] + B[gi, gj]
    return kern

M, N = 1024, 2048
A = torch.randn(M, N, device='cuda')   # shape: [M, N]
B = torch.randn(M, N, device='cuda')   # shape: [M, N]
C = torch.empty(M, N, device='cuda')   # shape: [M, N]
kernel = elementwise_2d(M, N)
kernel(A, B, C)
torch.testing.assert_close(C, A + B)
print("ok")
```

## 2.10 完整示例二：整行归约（softmax 的前半段）

第二个例子刻意换一种风格：这是一个**正确性基线**，先让每个输出元素只被写一次，再考虑
怎样提高并行度。这里把 `threads=1` 写出来，是为了让「串行归约」和「并行归约」形成清晰
对照；后者需要明确归约维度、线程间通信和结果写回。

```python
@jit
def row_sum(M: int, N: int, dtype: str = 'float32'):
    @T.prim_func
    def kern(
        A: T.Tensor((M, N), dtype),
        S: T.Tensor((M,), dtype),
    ):
        with T.Kernel(M, threads=1) as bx:
            acc = T.alloc_var(dtype)        # 教学基线：一个 block/线程处理一行
            acc = 0                          # 初始化（也可 T.alloc_var(dtype, init=0)）
            for j in T.serial(N):            # 串行归约；并行化需要跨线程归约原语
                acc += A[bx, j]
            S[bx] = acc
    return kern
```

> 这里故意把 `threads=1` 写清楚：这是正确但慢的串行基线，不是并行归约。
> 如果把线程数改大而不改变算法，多个线程会重复计算同一行；并行化必须使用
> `T.reduce_sum` 或 `T.warp_reduce_sum`，并验证归约维度与布局。性能写法见第 03/07 章。
>
> 这个基线还有一个真实教训：一项对 TileLang/Triton 内核的经验研究
> （["Correct but Slow"](https://arxiv.org/abs/2607.04454)，A100/GH200 实测）发现，
> 用 `T.serial` 写归约正是**能通过正确性检查却慢两个数量级以上**的头号原因：一个
> 惯用写法的 LayerNorm 过了 KernelBench 的正确性门禁，却比 PyTorch 慢 299 倍；把
> `T.serial` 循环换成 `T.reduce`（并保持原生 dtype 输入输出）后恢复了 1347 倍。
> 该研究的计数器证据是 `warp_stall_long_scoreboard`（等内存延迟）从 59.56 降到 ≈0，
> 而 `warp_stall_barrier` 始终 ≈0——说明瓶颈是串行访存等待，而不是同步。
> 结论：**「正确」的归约写法从第 02 章起就要分清 `T.serial`（教学对照）与
> `T.reduce`（并行归约）**；第 09 章会把这一计数器判据放进性能排查表。

## 2.11 调试打印

写出第一个能编译的内核之后，下一步往往是「为什么数值不对」，这时需要看到中间值。
`T.print` 是**设备端打印**，必须写在 `@T.prim_func` 的内核函数体内，不能像 `print()`
一样放在 host 侧 Python 代码中。它适合检查小尺寸输入下的中间值、索引和累加器状态。

```python
@T.prim_func
def debug_kernel(
    A: T.Tensor((M, N), 'float32'),
    C: T.Tensor((M, N), 'float32'),
):
    with T.Kernel(T.ceildiv(N, 64), T.ceildiv(M, 64), threads=128) as (bx, by):
        # 共享内存中的 tile：适合检查 global → shared 搬运是否正确。
        A_shared = T.alloc_shared((64, 64), 'float32')
        T.copy(A[by * 64, bx * 64], A_shared)

        # 只打印一个标量，避免每个线程/每个元素都输出大量日志。
        if bx == 0 and by == 0:
            T.print(A_shared[0, 0], msg='A_shared[0, 0] = ')

        # 片段或累加器：适合检查 GEMM/归约后的中间结果。
        C_frag = T.alloc_fragment((64, 64), 'float32')
        T.clear(C_frag)
        if bx == 0 and by == 0:
            T.print(C_frag[0, 0], msg='C_frag[0, 0] = ')

        # 标量变量：适合检查 max、sum、索引等状态。
        s = T.alloc_var('float32', init=0)
        if bx == 0 and by == 0:
            T.print(s, msg='scalar s = ')
```

上面示例假设调试输入的 `M/N` 是 64 的整数倍，因此省略了边界 guard；调试非整除尺寸时，
必须沿用前面的尾部保护或显式补零，不能直接照搬这段 tile 搬运代码。

调试时遵循下面的顺序：

1. 先把输入缩小到 `M/N <= 64`，并固定随机种子；
2. 只打印一个 block、一个线程或一个标量；
3. 把打印值与 PyTorch/NumPy 参考实现逐项比较；
4. 找到问题后立即删除 `T.print`，再重新 benchmark。

有两件事要一直记着：设备端打印会改变时序并产生大量输出，不能用于性能测量；不同后端的
输出格式和支持范围也可能不同。若结果错误，优先使用 `T.print` 检查「输入 tile → 中间结果 →
最终写回」这条数据链，而不是一开始打印整个 fragment。

## 2.12 Python 语法边界：长得像 Python，不等于都能执行

最后要说一个容易忽视的事实：TileLang 复用了 Python 语法来构造 TIR，所以判断一行代码是否
合法时，不能只看 Python 解释器能否解析，还要看 TileLang 是否定义了对应语义。

| 写法 | 在内核中的处理 | 更稳妥的做法 |
|---|---|---|
| `for i in range(n)` | 当前兼容层可映射为串行循环 | 教程显式写 `T.serial(n)`，让语义可见 |
| `if` / `else` | 支持，条件必须能转成 TIR 表达式 | 复杂合取用 `T.all_of` / `T.any_of` |
| `break` / `continue` | 可用于受支持的循环 | 保持后续循环体简单，并做小尺寸测试 |
| 普通 `print()` | 只打印 Python 表达式，不是设备调试 | 设备值使用 `T.print()` |
| `assert` | 不能照搬普通 Python 运行时假设 | 按当前版本使用 `T.device_assert` 或相应原语 |
| 任意对象、I/O、NumPy 调用 | 通常不能进入设备 IR | 放到 host 侧或改成 `T.*` 原语 |

遇到“Python 能运行，TileLang 却无法 lower”的情况，先把表达式缩到一个 `prim_func`，
再对照 Python Compatibility 页面。不要靠试错把 host 逻辑混进 kernel。

## 2.13 本章回顾

这一章把 TileLang 的最小语法模型立了起来：内核 = `@T.prim_func` 函数 + `T.Kernel`
启动配置 + 循环 + 搬运 + 计算。循环的选择有一条口诀——**elementwise 用 Parallel，
归约/串行依赖用 serial，小循环用 unroll，跨 tile 的主循环用 Pipelined**。内存上，
`alloc_shared`（block 共享）、`alloc_fragment`（寄存器片段，布局推断）和
`alloc_var`（标量）分别对应三档作用域。最后，dtype 三种写法等价，而混合精度
（fp16 输入 + fp32 累加）是常态。把这些读熟，第 03 章的内存层次就有了可以挂靠的锚点。

## 2.14 动手任务

完成下面任务再进入第 03 章：

1. 解释 `T.const`、Python 工厂参数和运行时张量形状分别在哪个阶段生效；
2. 把 2D elementwise 改成 `M=1003, N=257`，补上并验证尾部 guard；
3. 比较 `row_sum` 的串行基线和一个并行归约版本，报告正确性、延迟和归约维度；
4. 指出一个 `alloc_shared`、`alloc_fragment` 和 `alloc_var` 的适用场景。
5. 从一个 Python 函数中找出不能进入 `prim_func` 的 I/O 或库调用，并把它移到 host 侧。

## 自问自答

下面这些问题用来检验你是否能把这一章的知识讲出来（详答见第 10 章）：

1. **`T.Tensor((N,), dtype)` 的 `(N,)` 是什么？**（形状元组；也可符号化）。
2. **`T.Parallel` 与 `T.serial` 的区别？**（映射线程/向量化 vs 顺序执行）。
3. **`T.Kernel(grid, threads=...)` 与 CUDA 的对应关系？**（grid ↔ blockIdx，threads ↔ blockDim；线程循环映射编译器负责）。
4. **fragment 与 shared 的区别？**（寄存器、每线程一份 vs 片上共享、block 可见）。
5. **为什么累加器常用 fp32？**（数值稳定 + 匹配 Tensor Core 累加精度）。
6. **为什么要 `T.ceildiv` 而不是整除？**（grid 必须覆盖余数 tile）。
7. **`break/continue/while` 支持吗？**（支持，条件是 TIR 表达式，见第 04 章）。
