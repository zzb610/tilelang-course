# 第 04 章 软件流水线与控制流

> **本章目标**：把"循环与分支"玩到专家级别：四种循环的选择、边界处理、以及
> TileLang 最值钱的能力之一——**软件流水线**（自动推断 + 手工注解 + 手动异步）。
> 这是 GEMM 跑得快的真正原因，也是面试最细的考点。

**学习信息**

- 难度：中级；预计用时：3–4 小时；
- 前置：第 02～03 章，理解并行循环、shared memory 和数据依赖；
- 运行范围：`T.Pipelined` 主线以 CUDA GEMM 为目标；`async_copy`/PTX wait 属于 CUDA 专项；
- 本章产出：一张 prologue/steady-state/epilogue 时序图，以及一份 pipeline 参数实验记录。
- 参考：[Software Pipeline Guide](https://github.com/tile-ai/tilelang/blob/main/docs/programming_guides/software_pipeline.md)。

## 4.1 循环族谱：选型就是性能决策

| 循环 | 语义 | 硬件效果 | 用在哪 |
|---|---|---|---|
| `T.serial(a, b, s)` | 顺序循环 | 普通 for | 有串行依赖（归约）、跨 tile 的 K 维主循环（手动流水） |
| `T.unroll(a, b, s)` | 请求展开 | 直列代码，省分支开销 | 小常数循环、指令级并行提升 |
| `T.Parallel(e0, e1, …)` | 嵌套并行 | 映射线程 + 向量化 | elementwise、tile 内拷贝 |
| `T.Pipelined(n, num_stages=k)` | 自动流水线 | 拷贝与计算重叠 | GEMM/Attention 的 K 维主循环（首选） |
| `T.Persistent(...)` | 持久化线程块 | 一个 block 跑多轮 | 高级模板（warp-specialize、Stream-K） |

```python
for i in T.serial(N): pass
for i in T.serial(0, N, 2): pass          # start/stop/step 三参数形式
for k in T.unroll(K_TILE): acc += A[k]*B[k]
for i, j in T.Parallel(M, N): C[i,j] = ...
```

`T.Parallel` 可选提示：

```python
for i, j in T.Parallel(M, N, coalesced_width=32):   # 提示合并宽度（向量化检查用）
    ...
```

## 4.2 条件、while 与 break

```python
for i in T.serial(N):
    if i < N:
        C[i] = A[i] + B[i]
    else:
        pass

x = A[i] if i < N else 0        # 三目

if T.all_of(gi < M, gj < N):    # 多条件（T.any_of 同理）
    C[gi, gj] = ...

i = 0
while i < N:                    # while 条件必须是 TIR 表达式
    ...
    if done:
        break
    i += 1
```

- Python 的 `break`/`continue` 在 `serial/unroll/Parallel/while` 中均可用；
- Python 常量布尔会在编译期折叠（`if True:` 直接展开）；
- 编译器会检测"恒真"的 while 循环并报错，注意别写死循环。

### 4.2.1 边界（残差 tile）处理模式

当问题尺寸不是 tile 的整数倍时，经典写法：

```python
for i, j in T.Parallel(BM, BN):
    gi, gj = by * BM + i, bx * BN + j
    if T.all_of(gi < M, gj < N):
        C[gi, gj] = A[gi, gj] + B[gi, gj]
```

好消息：`LegalizeSafeMemoryAccess` pass 会自动为「可能越界」的访问插入保护，
能被证明安全时自动消掉。所以多数情况下**不写 if 也能跑对**；需要自定义边界逻辑
或追求清晰时才显式写。

## 4.3 软件流水线：把等待变成计算

### 4.3.1 问题：为什么朴素 GEMM 慢？

K 维循环里"拷 A/B tile → 计算"是串行的：计算必须在拷贝完成后开始。每次拷贝
global→shared 的延迟（几百周期）都暴露在关键路径上。

### 4.3.2 解决：流水线重叠

把拷贝提前：第 i 轮拷数据的同时，算第 i-1 轮拷好的数据。结构上分为三段：

```text
prologue     : 预取 stage0、stage1 的数据
steady-state : 每一轮：拷贝 stage k+1 的数据 + 计算 stage k 的数据（重叠！）
epilogue     : 算掉最后留在管道里的数据
```

用 `T.Pipelined` 一句话完成（num_stages=3 表示 3 级缓冲）：

```python
for ko in T.Pipelined(T.ceildiv(K, BK), num_stages=3):
    T.copy(A[by * BM, ko * BK], A_s)   # 生产者：global → shared（可异步）
    T.copy(B[ko * BK, bx * BN], B_s)
    T.gemm(A_s, B_s, C_f)              # 消费者：共享内存 → 计算
```

编译器会自动：给 `A_s/B_s` 做多级缓冲（double/triple buffering）、把拷贝调度成
异步、插入必要的同步。**这是 GEMM 的标准写法，第 05 章全程用它。**

### 4.3.3 `num_stages` 怎么选？

- 越大重叠越深，但共享内存消耗线性增长（每级一份 tile）；
- 共享内存上限：如 48KB 默认（可动态申请更大），fp16 的 128×32 tile = 8KB，
  3 级 = 24KB × 2（A、B），接近满；128×128 acc 在寄存器里；
- 常见取值 2~4；autotune 就是干这个的（第 08 章）。

## 4.4 手工注解：`stage` 与 `order`

自动推断覆盖大多数情况；非标准结构（加后处理、异步归组、重排）需要手写：

```python
for ko in T.Pipelined(
    T.ceildiv(K, BK),
    stage=[0, 0, 1],   # 与语句顺序对齐：copy A=0, copy B=0, gemm=1
    order=[0, 1, 2],
):
    T.copy(A[ko * BK], A_shared)
    T.copy(B[ko * BK], B_shared)
    T.gemm(A_shared, B_shared, C_local)
```

规则（官方明确）：

- `stage`/`order` 只标注**有副作用的调度语句**（copy/fill/gemm/reduce/store/原子/
  显式 wait/commit/同步），且按源代码顺序对齐；
- 手工指定 `stage/order` 时**不要再传 `num_stages`**，深度自动取
  `max(stage)+1`；
- 生产者必须不晚于消费者：同 stage 内 producer 的 order 更小；跨 stage 时
  producer 的 stage ≤ consumer 的 stage。

重排示例（先发下轮生产者、后发本轮消费者）：

```python
for ko in T.Pipelined(num_tiles, stage=[0, 1], order=[1, 0]):
    T.copy(A[ko * BK], A_shared)
    T.gemm(A_shared, B_shared, C_local)
    # copy → stage0 order1；gemm → stage1 order0；发射时先 copy 再 gemm（跨轮）
```

### 4.4.1 关键铁律：replayable 标量 Bind 不占注解槽位

像 `base: T.int32 = ko * BK` 这类**纯值别名**（无副作用、无存储），不算调度语句：

```python
for ko in T.Pipelined(num_tiles, stage=[0, 1], order=[1, 0]):
    base: T.int32 = ko * BK            # ← 不占 stage/order 槽位！
    T.copy(A[base + tx], A_shared[tx])
    B[base + tx] = A_shared[tx]
```

- 编译器按**使用处驱动重放**：哪个消费者需要，就在它前面重新生成这个别名
  （因此跨 stage 使用也正确，只是可能重复计算）；
- 只有「读管道内被写缓冲」的 Bind 才需要算调度语句（它有真实依赖）；
- 想"只 load 一次"，请显式写入 buffer（`idx_shared[tx] = Ids[ko]`），那才是
  真正的生产者语句。

## 4.5 手动异步流水线：cp.async 与 `T.ptx_wait_group`

`T.Pipelined` 之外，`T.async_copy` 给你最细粒度控制（专家向）：

```python
def manual_async_fragment(A, A_s, B_s, C_f, by, ko, BM, BK):
    # 预取下一轮数据（发出异步拷贝，不等完成，继续做事）
    T.async_copy(A[by * BM, ko * BK], A_s)   # 具体分组由目标/版本决定

    # ……中间可以插入与 A_s 无关的计算……

    # 消费前必须显式等待
    T.ptx_wait_group(0)

    T.gemm(A_s, B_s, C_f)
```

语义对照：

| API | 语义 | 需要手动 wait 吗 | 备注 |
|---|---|---|---|
| `T.copy` | 同步 | 不需要 | 即使内部降级为 cp.async 也会自动 commit/wait |
| `T.async_copy` | 显式异步 | **消费前必须确认已完成**；CUDA 常用 wait group | 目标/版本不支持时可能编译失败；以官方 guide 为准 |

> 注意：`async_copy` 只保证"数据到达 shared"，跨线程消费仍需共享内存屏障；
> 默认流水（`ThreadSync("shared")` pass）会在首次读 dst 前自动插入
> `T.tvm_storage_sync("shared")`，所以常规写法不用手写。TMA（`T.tma_copy`，
> `cp.async.bulk`）在第 07 章讲。

> 这段是 CUDA 专项语义：`wait_group` 的编号、提交方式和屏障插入都可能随目标与版本变化。
> 练习时先用 `T.Pipelined` 验证正确性，再用生成源码确认异步指令和 wait 是否真的出现。

## 4.6 一个综合例子：带残差处理的流水线 elementwise

把所学串起来：非整除边界 + 流水线语义（此例计算简单，真正的流水线收益在
GEMM/FA 里，这里展示组合写法）：

```python
@T.prim_func
def tile_add(A: T.Tensor((M, N), 'float32'), C: T.Tensor((M, N), 'float32')):
    with T.Kernel(T.ceildiv(N, BN), T.ceildiv(M, BM), threads=256) as (bx, by):
        A_s = T.alloc_shared((BM, BN), 'float32')
        T.copy(A[by * BM, bx * BN], A_s)
        for i, j in T.Parallel(BM, BN):
            gi, gj = by * BM + i, bx * BN + j
            if T.all_of(gi < M, gj < N):
                C[gi, gj] = A_s[i, j] * 2.0
```

## 4.7 常见错误与排查

| 症状 | 原因 | 修法 |
|---|---|---|
| 结果错误且只在边界 tile | 越界读写或尾部 tile 未零填充 | 输出显式 `T.all_of` guard；GEMM 输入显式 padding/填 0 |
| 手工注解报"statement count mismatch" | stage/order 长度不对齐 | 只数有副作用的语句，去掉 Bind |
| 共享内存超限编译失败 | `num_stages` 太大 / tile 太大 | 减小 stages 或 tile；检查 `T.alloc_shared` 总量 |
| 用了 `async_copy` 却结果不对 | 忘了 `ptx_wait_group` | 消费前显式等 0 | 
| 预测性错误：等待组数不对 | 多个 async group 混用 | 理清每个 group 的边界，必要时用 `T.ptx_commit_group` |

## 4.8 本章小结

- 循环选型：elementwise→Parallel；串行依赖→serial；小循环→unroll；跨 tile 主循环→Pipelined。
- 软件流水线 = 拷贝与计算重叠，`num_stages` 调深度；手工调节奏用 `stage/order`。
- `async_copy` 是显式异步，必须自己 `ptx_wait_group`；`T.copy` 永远同步语义。
- 标量 Bind 是"值别名"，不占 pipeline 注解槽位，按需重放。

## 4.9 Checkpoint

1. 为一个 5 轮 tile 循环画出串行版和 3-stage 版的 prologue/steady-state/epilogue；
2. 说明 `stage/order` 为什么只标注有副作用的调度语句；
3. 让一个 GEMM/拷贝示例分别使用 `num_stages=2/3/4`，记录 shared memory 代价和延迟；
4. 人为制造一个尾部 tile，确认输出 guard/填充值，而不是只看“编译通过”。

## 面试考点（本章相关）

1. **什么是软件流水线？GEMM 为什么需要它？**（隐藏 global→shared 延迟，prologue/steady/epilogue）。
2. **`num_stages=3` 的代价？**（共享内存翻倍、寄存器压力；O(stages·tile)）。
3. **`T.copy` 与 `T.async_copy` 的区别？**（同步语义 vs 显式异步 + 手动 wait）。
4. **手工 stage/order 何时需要？规则？**（非标准 body；只标注副作用语句，producer 不晚于 consumer）。
5. **`cp.async` 是什么？`commit/wait group` 机制？**（异步拷贝到共享内存，按组管理完成状态）。
6. **while/break/continue 支持吗？边界保护是自动的吗？**（支持；LegalizeSafeMemoryAccess 自动插桩）。
7. **为什么流水线深度不能无限大？**（共享内存/寄存器容量、启动开销）。
