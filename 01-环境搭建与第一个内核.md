# 第 01 章 环境搭建与第一个内核

学习 GPU 编程的第一个困难，通常不是并行算法，而是区分主机端的 Python 构造过程与设备端
的内核执行过程。若不先分清这两个阶段，安装错误、编译
错误和内核运行错误就会混在一起，后面的性能分析也无从谈起。

框架里的向量加法只要一行 `A + B`。我们偏选这个最简单的算子，是想一次看清一个 Python
函数如何变成 TIR、后端源码和可调用内核，完整走一遍“构造—编译—执行—验证”四个阶段。
先不追求速度：只有证明了内核正确，后面测性能才有意义。

本章默认你已会用 Python、张量和基本命令行。主线面向 CUDA GPU；没有 GPU 也能读完生成
过程、走 compile-only 路径，但只靠这些没法判断设备的正确性与性能。

> **本章导航** 入门难度，预计 2–3 小时；前置是 Python、张量/数组与基本命令行。
> 主线需要 CUDA GPU，没有 GPU 也可以完成阅读、静态检查和部分 host 侧准备。学完你会留下
> 一个带正确性校验的 vector add，以及一份环境检查记录。写作参考：[Installation Guide](https://tilelang.com/get_started/Installation.html)、
> [Understanding Targets](https://tilelang.com/get_started/targets.html) 和
> [TileLang Getting Started](https://tilelang.com/get_started/overview.html)。

## 1.1 先确认我们正在面对哪种环境

TileLang 面向 GPU 高性能计算，先把环境预期对齐：

| 项目     | 要求                              | 说明                                                      |
| -------- | --------------------------------- | --------------------------------------------------------- |
| Python   | 当前官方文档要求 3.10 或更高      | 最终仍以所装 wheel 的元数据和本地安装结果为准             |
| 操作系统 | Linux/Windows；macOS 走 Metal     | Linux wheel 还要满足 glibc 要求；教程主线示例面向 CUDA    |
| GPU      | NVIDIA CUDA 为主                 | AMD/Metal 只能按当前版本支持范围运行；无 GPU 可学 DSL 语法，但不能完成 GPU 运行实验 |
| 库       | PyTorch（建议 CUDA 版）           | 用于准备张量、正确性校验与基准对比                        |
| 基础     | Python、NumPy/Tensor 概念         | 懂一点 CUDA/GPU 概念更好，但本课程会从零讲                |

> 哪一项还没就绪，不必急着背 API。先记下缺什么、哪些实验能离线做，再看看是补本地
> 环境，还是直接用云 GPU。

### 1.1.1 无 GPU 时怎么做

没有 GPU，这一章也不用跳过。语法学习完全不受影响：第 02、03 章的概念照常读，代码可以
先写好、不运行。你还能用官方 compile-only 工具把脚本 lower 成 C/CUDA 等目标源码——它能
检查解析和部分 lowering 路径，但证明不了设备运行正确、也证明不了性能达标。macOS 用户可以
跑 `examples/metal_verify.py` 做基础 smoke test，但要记住：Metal 的 API 覆盖面和教程示例
（CUDA）有差异，别拿 Metal 的结果去下 CUDA 的性能结论。真要做完整实验，就选一套与当前
PyTorch/TileLang 版本匹配的云 GPU；不要只按显卡型号挑环境，驱动、CUDA、PyTorch 和
TileLang 版本也一样要记下来。

## 1.2 让主机、TileLang 与设备彼此可见

截至 TileLang 0.1.13，官方 wheel 安装说明要求 Python 3.10 或更高；Linux 预编译包要求
glibc 2.28。CUDA 工具链有「使用主机安装」和「由 pip 环境提供」两条路径，版本组合不同。
这些条件会变化，安装失败时先回到官方 Installation Guide，不要混用旧博客中的版本下限。

### 方式一：pip 安装（推荐，最快）

```bash
python -m venv .venv
source .venv/bin/activate       # Windows 使用 .venv\\Scripts\\activate
python -m pip install -U pip
pip install tilelang
pip install torch                # 选择与目标设备匹配的 PyTorch 构建
```

装完验证：

```bash
# 确认 TileLang 已安装，并打印当前版本。
python -c "import tilelang; print(tilelang.__version__)"
```

### 方式二：安装最新版 / nightly

```bash
# 从 GitHub 源码直接安装
pip install git+https://github.com/tile-ai/tilelang.git

# nightly 构建（体验最新特性，可能不稳定）
pip install tilelang -f https://tile-ai.github.io/whl/nightly
```

### 方式三：从源码构建（开发/研究用）

```bash
git clone --recursive https://github.com/tile-ai/tilelang.git
cd tilelang
pip install -r requirements-dev.txt
pip install -e . -v --no-build-isolation   # 开发模式；仓库含所需 submodule
```

源码构建时，常用的 CMake 选项如下：

| 选项               | 作用                                       |
| ------------------ | ------------------------------------------ |
| `-DUSE_CUDA=ON`  | 启用 CUDA 支持（找到 CUDA 头文件时默认开） |
| `-DUSE_ROCM=ON`  | 构建 AMD ROCm 后端                         |
| `-DUSE_LLVM=ON`  | 启用 LLVM（CPU 代码生成）                  |
| `-DUSE_METAL=ON` | macOS 上默认开启 Metal 后端                |

> 小贴士：源码构建推荐 `cmake -G Ninja && ninja` 而不是 `make`，重编译快得多。
> 改 C++ 代码后只需在 build 目录再跑一次 `ninja`；改 Python 代码用
> `pip install -e .` 开发模式即可即时生效。

### 方式四：Docker（环境一致，团队协作友好）

```bash
# 进入仓库中的 Docker 配置目录。
cd tilelang/docker
# 构建 CUDA 12.0 镜像；首次构建会下载依赖，耗时较长。
docker build -f Dockerfile.cu120 -t tilelang-cu120 .
# 启动带 GPU、共享内存和当前目录挂载的交互容器。
docker run -itd --gpus all --shm-size 32g -v $PWD:/workspace --name tl tilelang-cu120 /bin/zsh
```

### 1.2.1 安装后检查清单

```bash
# 在容器或虚拟环境中检查 TileLang、PyTorch 和 CUDA 是否可用。
python -c "
import tilelang, torch
print('tilelang', tilelang.__version__)
print('torch', torch.__version__)
print('cuda available:', torch.cuda.is_available())
"
```

编译缓存默认在 `~/.tilelang/cache`（可用环境变量 `TILELANG_CACHE_DIR` 修改）。
调试时可临时 `TILELANG_DISABLE_CACHE=1` 强制重编译。

### 1.2.2 先选 target，再解释结果

TileLang 是跨后端的 DSL，当前文档列出的常见 target 远不止 CUDA。动手前先把这张表通读
一遍，它划定了「结果能说明什么」的边界：

| target | 用途 | 本教程中的边界 |
|---|---|---|
| `auto` | 按 CUDA → HIP → Metal 的顺序探测 | 便于运行，不适合需要固定架构的性能报告 |
| `cuda` | NVIDIA GPU | 主线 target；架构相关优化必须记录 `sm_*` |
| `hip` | AMD GPU | 需要 ROCm 构建；不能套用 CUDA 指令结论 |
| `metal` | Apple Silicon GPU | 可做基础与当前支持的 GEMM 实验，单独报告结果 |
| `llvm` | CPU | 用于 CPU codegen，不代表 GPU 行为 |
| `webgpu` / `c` | 浏览器或源码输出 | 适合检查可移植性与生成代码 |
| `cutedsl` | NVIDIA CuTe DSL backend | 需要额外依赖，属于版本敏感路径 |

简单实验用字符串即可；需要固定 GPU 架构时，改用 target 配置字典：

```python
target = {"kind": "cuda", "arch": "sm_90"}
jit_kernel = tilelang.compile(func, target=target, out_idx=[2])
```

如果出现 `no kernel image is available`，优先检查 `arch` 是否与实际 GPU 匹配。性能调优
结果也要把 target 配置和设备型号一起归档；`target="auto"` 只说明探测成功，不说明后端等价。

没有 GPU 时，可以先检查生成阶段是否走得通：

```bash
python -I -m tilelang.tools.compile_only --target cuda \
  --output_file out.cu example.py
```

这个工具默认选文件里第一个 `@tilelang.jit` kernel 或 `PrimFunc`。它不启动设备，也覆盖不到
运行期边界、同步和数值误差测试。

## 1.3 从 Python 函数到设备内核

下面使用一个官方 Quickstart 风格的例子，并按编译阶段逐层分析。

### 1.3.1 完整代码

```python
import torch
import tilelang
import tilelang.language as T
from tilelang import jit

@jit  # 外层装饰器：生成可调用内核；首次用实际张量调用时绑定 target/形状并编译
def add(N: int, block: int = 256, dtype: str = 'float32'):

    @T.prim_func  # 把下面的 Python 函数“翻译”成 TIR（TVM 中间表示）
    def add_kernel(
        A: T.Tensor((N,), dtype),
        B: T.Tensor((N,), dtype),
        C: T.Tensor((N,), dtype),
    ):
        # T.Kernel 声明启动配置：一维网格（需要 N/block 个 block），每 block block 个线程
        with T.Kernel(T.ceildiv(N, block), threads=block) as bx:
            # T.Parallel(block) 表示这层循环由 block 内线程并行执行
            for i in T.Parallel(block):
                gi = bx * block + i          # 全局索引：blockIdx.x * blockDim.x + threadIdx.x
                if gi < N:
                    C[gi] = A[gi] + B[gi]    # 显式处理尾部 tile

    return add_kernel

# ---- 主机侧：准备数据并调用 ----
N = 1 << 20  # 1048576
A = torch.randn(N, device='cuda', dtype=torch.float32)   # shape: [N]
B = torch.randn(N, device='cuda', dtype=torch.float32)   # shape: [N]
C = torch.empty(N, device='cuda', dtype=torch.float32)   # shape: [N]

kernel = add(N)      # 构造特化版本；首次用张量调用时触发编译
kernel(A, B, C)      # 在 GPU 上执行

torch.testing.assert_close(C, A + B)   # 与 PyTorch 参考实现对比
print("✓ 向量加法内核正确！")
```

### 1.3.2 一段代码，五个生效阶段

这段代码一共五层，从上往下正好是从「Python 层面」走到「设备执行」的过程。

1. **`@T.prim_func`**：把 Python 函数体翻译成 TIR（TVM IR 的一种）。参数上的
   `T.Tensor((N,), dtype)` 声明了全局内存中的张量形状与数据类型。此刻**还没有任何
   CUDA 代码**，只有一份 IR。
2. **`with T.Kernel(...)`**：声明「启动配置」——`ceildiv(N, block)` 个线程块、每块
   `block` 个线程。`bx` 就是 CUDA 里的 `blockIdx.x`。注意：**你不需要手写
   `threadIdx.x`**，编译器会把 `T.Parallel` 循环自动映射到线程。
3. **`T.Parallel(block)`**：被标记为可并行的循环。编译器决定如何把它切分到
   `threads=block` 个线程上，并尝试向量化（见 1.4）。
4. **边界安全**：`N` 不一定是 `block` 的整数倍，最后一个 block 可能越界。我们在
   入门示例中显式写 `if (gi < N)`，因为它同时表达了算法语义。TileLang 的
   `LegalizeSafeMemoryAccess` 可能为可疑访问插入 guard，但 guard 的存在不代表越界
   读取会自动得到 0；GEMM/Attention 的尾部 tile 仍要设计 padding 或有效元素逻辑。
5. **`@jit`（`from tilelang import jit`）**：装饰在外层工厂函数 `add` 上。构造并
   首次用实际张量调用 `kernel` 时，会解析 `prim_func` → 跑编译流水（第 09 章详讲）
   → 生成目标源码 → 调用对应编译/运行后端 → 返回可调用模块。之后在进程内通常
   命中**编译缓存**，不再重复编译。`N`、`block`、`dtype` 这些 Python 参数在
   编译期被「烘焙」进 TIR——这也是 TileLang 最常见的模板化方式（`block`、tile 尺寸
   等调优参数都从这里传入）。

### 1.3.3 查看生成的内核源码（建议至少做一次）

TileLang 有个很实用的特点：能直接看到生成的后端源码。运行下面两行：

```python
# 读取编译后的 CUDA/HIP/Metal 源码，观察 TIR 最终如何落到后端。
source = kernel.get_kernel_source()
# 打印源码，便于检查索引、边界 guard 和向量化结果。
print(source)
```

输出中会出现形如 `__global__ void ...` 的 CUDA 代码；具体是否循环展开、向量化、使用
何种边界 guard，取决于目标、版本和形状。把 TileLang 代码和生成代码对照着看，
是理解这个 DSL 如何落到后端的有效方法。

### 1.3.4 性能基线

正确的下一步是测量，而不是优化。用 profiler 记一次基线延迟，并换算成带宽利用率：

```python
profiler = kernel.get_profiler()
latency_ms = profiler.do_bench()          # 默认基于 CUDA event 多次计时取平均
print(f"vector add latency: {latency_ms:.3f} ms")

# 理论显存带宽利用率（把 peak_bandwidth 替换成目标 GPU 的官方规格）
bytes_moved = 3 * N * 4                    # 读 A、读 B、写 C
peak_bandwidth = 2.0                       # TB/s；示例值，不是课程常数
bandwidth = bytes_moved / (latency_ms * 1e-3) / 1e12
utilization = bandwidth / peak_bandwidth
print(f"achieved bandwidth: {bandwidth:.2f} TB/s ({utilization:.1%} of the chosen peak)")
```

这类带宽受限（memory-bound）内核的主要限制通常在数据搬运，而不是加法本身；但光说「受限」
还不行，得用实测带宽和 profiler 来验证。vector add 的计算强度很低，每个元素只做一次加法，
可优化的重点自然落在访问连续性、启动开销和数据传输上，而不是增加算术指令。

## 1.4 一个练习：改成 int32 并加边界

动手是最好的检验。把 `dtype` 换成 `'int32'`，把 `N` 设成不是 `block` 整数倍的值
（如 `N = 1000000`），再跑一次验证。如果正确性报错，回忆 1.3.2 第 4 点——越界保护是
自动的，但**逻辑上自己的索引计算**（如 `gi`）仍要保证正确。

## 1.5 不同的编译入口：`tilelang.compile` 与 `@jit` 对比

TileLang 有两个入口，它们回答的是同一个问题——「一段 Python 内核如何变成可调用对象」，
但控制粒度不同：

| 方式                                                     | 适用                                                               | 示例             |
| -------------------------------------------------------- | ------------------------------------------------------------------ | ---------------- |
| `@tilelang.jit`                                        | 文档/原型/模板化内核；从实际张量推断目标与形状                     | 本教程大部分代码 |
| `tilelang.compile(func, out_idx=[...], target="cuda")` | 显式控制 target、指定输出参数（`out_idx`）让宿主自动分配输出张量 | 官方 matmul 教程 |

```python
# compile 风格：out_idx=[2] 表示第 3 个参数是输出，调用时不必传入
jit_kernel = tilelang.compile(func, out_idx=[2], target="cuda")
c = jit_kernel(a, b)   # 返回输出张量，而不是改写到第三个参数
```

`target` 的完整范围和配置字典见 1.2.2；执行后端（execution backend）有
`tvm_ffi` / `cython` / `nvrtc` / `torch` 等，一般用 `'auto'` 即可。target 决定生成什么
设备代码，execution backend 决定宿主侧怎样编译、装载和调用，两者别混为一谈。

## 1.6 本章回顾

这一章完整经历了安装、编译、执行与验证。安装方面，`pip install tilelang` 是最快的方法；没有 GPU 时
可以学语法、做 compile-only 检查，但运行与性能结论仍需要目标设备。语言上，一个最小
内核由 `@T.prim_func` + `with T.Kernel(...)` + 循环组成，`@jit` 负责编译与缓存，
`T.Parallel` 让编译器替你分配线程与向量化——但边界语义仍应在内核或 padding 设计中明确。
最后养成三个必做动作：`get_kernel_source()` 看生成代码、`do_bench()` 测性能、
`torch.testing.assert_close` 验正确性。后续章节会继续使用这三种方法。

## 1.7 动手任务

下面的实验用来确认你已经分清构造、编译与执行。它们不是进入下一章的形式门槛，而是下一章
讨论语言模型时所需的经验材料。完成下面任务再进入第 2 章：

1. 将 `N` 改成 `1000003`，确认尾部输出正确；
2. 再使用 `N=1` 和 `N=257` 测试极小输入和非整除输入；
3. 保存 `get_kernel_source()` 的一小段，并指出 kernel 参数、线程索引和边界 guard；
4. 记录一次 `do_bench()` 的延迟，同时写出硬件、dtype、warmup/rep 和带宽公式。
5. 打印或显式设置一次 target，说明 target 与 execution backend 的分工。

## 1.8 自问自答

下面这些问题用来检验你是否能把这一章的知识讲出来（详答见第 10 章）：

1. **TileLang 和纯 Python 的 `@T.prim_func` 有什么关系？**（AST/解析成 TIR，不是逐行执行）。
2. **`T.Kernel` 与 CUDA 的 grid/block 如何对应？**（`bx/by/bz` ↔ `blockIdx`；`threads` ↔ `blockDim`）。
3. **为什么 TileLang 能「替你分配线程」？**（循环 → 线程映射由编译器完成，见第 03、09 章）。
4. **`@jit` 装饰器是做什么的？**（JIT 编译 + 缓存；Python 参数在编译期烘焙进 IR）。
5. **vector add 更可能受什么限制？如何验证？**（提出带宽受限假设，再用实测带宽和 profiler 验证）。
6. **`get_kernel_source()` 有什么用？**（理解抽象层和排查生成代码）。
