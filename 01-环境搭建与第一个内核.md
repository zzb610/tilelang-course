# 第 01 章 环境搭建与第一个内核

> **本章目标**：装好 TileLang、跑通第一个内核（向量加法）、理解"写内核 → 编译 → 运行 → 验证 → 分析"的完整闭环。学完本章，你就有了继续前进的"实验台"。

## 1.1 前置准备

TileLang 面向 GPU 高性能计算，建议具备：

| 项目     | 要求                              | 说明                                                      |
| -------- | --------------------------------- | --------------------------------------------------------- |
| Python   | ≥ 3.10                           | 官方 pip 包的硬性要求                                     |
| 操作系统 | Linux 优先（Ubuntu 20.04+）       | macOS（Metal 后端）、Windows 亦可，但教程示例面向 CUDA    |
| GPU      | NVIDIA（CUDA ≥ 10.0 主机工具链） | AMD GPU 走 ROCm；无 GPU 也能学 DSL 语法，只是无法运行内核 |
| 库       | PyTorch（建议 CUDA 版）           | 用于准备张量、正确性校验与基准对比                        |
| 基础     | Python、NumPy/Tensor 概念         | 懂一点 CUDA/GPU 概念更好，但本课程会从零讲                |

> 面试官视角：能流畅说出"为什么需要 GPU 内核 DSL"的人，比只会调库的人强得多。
> 本课程的每一个概念都会解释到"面试能讲清楚"的程度。

### 1.1.1 没有 GPU 怎么办？

- **语法学习不受影响**：第 02、03 章的概念可以照常阅读，代码可以先写好不运行。
- macOS 用户可尝试 Metal 后端（构建时 `USE_METAL=ON`，默认在 Darwin 上开启），
  但 API 覆盖面与教程示例（CUDA）有差异。
- 云 GPU（如 AutoDL、AWS）按小时租一个 T4 即可覆盖本课程全部实验，成本很低。

## 1.2 安装 TileLang

### 方式一：pip 安装（推荐，最快）

```bash
pip install tilelang
```

装完验证：

```bash
python -c "import tilelang; print(tilelang.__version__)"
```

### 方式二：安装最新版 / nightly

```bash
# 从 GitHub 源码直接安装
pip install git+https://github.com/tile-ai/tilelang.git

# nightly 构建（体验最新特性，可能不稳定）
pip install tilelang -f https://tile-ai.github.io/whl/nightly
```

### 方式三：从源码构建（开发/研究用）

```bash
git clone --recursive https://github.com/tile-ai/tilelang.git
cd tilelang
pip install . -v        # 依赖自定义版 TVM（仓库自带 submodule）
```

常用 CMake 选项（源码构建时）：

| 选项               | 作用                                       |
| ------------------ | ------------------------------------------ |
| `-DUSE_CUDA=ON`  | 启用 CUDA 支持（找到 CUDA 头文件时默认开） |
| `-DUSE_ROCM=ON`  | 构建 AMD ROCm 后端                         |
| `-DUSE_LLVM=ON`  | 启用 LLVM（CPU 代码生成）                  |
| `-DUSE_METAL=ON` | macOS 上默认开启 Metal 后端                |

> 小贴士：源码构建推荐 `cmake -G Ninja && ninja` 而不是 `make`，重编译快得多。
> 改 C++ 代码后只需在 build 目录再跑一次 `ninja`；改 Python 代码用
> `pip install -e .` 开发模式即可即时生效。

### 方式四：Docker（环境一致，团队协作友好）

```bash
cd tilelang/docker
docker build -f Dockerfile.cu120 -t tilelang-cu120 .
docker run -itd --gpus all --shm-size 32g -v $PWD:/workspace --name tl tilelang-cu120 /bin/zsh
```

### 1.2.1 安装后检查清单

```bash
python -c "
import tilelang, torch
print('tilelang', tilelang.__version__)
print('torch', torch.__version__)
print('cuda available:', torch.cuda.is_available())
"
```

编译缓存默认在 `~/.tilelang/cache`（可用环境变量 `TILELANG_CACHE_DIR` 修改）。
调试时可临时 `TILELANG_DISABLE_CACHE=1` 强制重编译。

## 1.3 第一个内核：向量加法

我们写一个官方 Quickstart 风格的例子，把每个环节拆开讲。

### 1.3.1 完整代码

```python
import torch
import tilelang
import tilelang.language as T
from tilelang import jit

@jit  # 外层装饰器：编译 + 生成可调用内核；首次调用时根据张量推断 target 和形状
def add(N: int, block: int = 256, dtype: str = 'float32'):

    @T.prim_func  # 把下面的 Python 函数“翻译”成 TIR（TVM 中间表示）
    def add_kernel(
        A: T.Tensor((N,), dtype),
        B: T.Tensor((N,), dtype),
        C: T.Tensor((N,), dtype),
    ):
        # T.Kernel 声明启动配置：一维网格（需要 N/block 个 block），每 block block 个线程
        with T.Kernel(T.ceildiv(N, block), threads=block) as bx:
            # T.Parallel(block) 表示这层循环由 block 内线程并行执行
            for i in T.Parallel(block):
                gi = bx * block + i          # 全局索引：blockIdx.x * blockDim.x + threadIdx.x
                C[gi] = A[gi] + B[gi]        # 越界时编译器会自动插入保护（LegalizeSafeMemoryAccess）

    return add_kernel

# ---- 主机侧：准备数据并调用 ----
N = 1 << 20  # 1048576
A = torch.randn(N, device='cuda', dtype=torch.float32)
B = torch.randn(N, device='cuda', dtype=torch.float32)
C = torch.empty(N, device='cuda', dtype=torch.float32)

kernel = add(N)      # 第一次调用触发 JIT 编译，之后命中缓存
kernel(A, B, C)      # 在 GPU 上执行

torch.testing.assert_close(C, A + B)   # 与 PyTorch 参考实现对比
print("✓ 向量加法内核正确！")
```

### 1.3.2 这段代码发生了什么？（逐层拆解）

1. **`@T.prim_func`**：把 Python 函数体翻译成 TIR（TVM IR 的一种）。参数上的
   `T.Tensor((N,), dtype)` 声明了全局内存中的张量形状与数据类型。此刻**还没有任何
   CUDA 代码**，只有一份 IR。
2. **`with T.Kernel(...)`**：声明"启动配置"——`ceildiv(N, block)` 个线程块、每块
   `block` 个线程。`bx` 就是 CUDA 里的 `blockIdx.x`。注意：**你不需要手写
   `threadIdx.x`**，编译器会把 `T.Parallel` 循环自动映射到线程。
3. **`T.Parallel(block)`**：被标记为可并行的循环。编译器决定如何把它切分到
   `threads=block` 个线程上，并尝试向量化（见 1.4）。
4. **边界安全**：`N` 不一定是 `block` 的整数倍，最后一个 block 可能越界。官方 pass
   `LegalizeSafeMemoryAccess` 会自动插入 `if (gi < N)` 保护；能被证明安全时又会
   自动去掉——**你不用手动写边界判断**（写也行，见第 04 章）。
5. **`@jit`（`from tilelang import jit`）**：装饰在外层工厂函数 `add` 上。首次调用
   `add(N)` 时：解析 `prim_func` → 跑编译流水（第 09 章详讲）→ 生成 CUDA 源码 →
   调用 nvcc/rtc 编译成可执行模块 → 返回一个可调用对象 `kernel`。之后在进程内
   直接命中**编译缓存**，不再重复编译。`N`、`block`、`dtype` 这些 Python 参数在
   编译期被"烘焙"进 TIR——这也是 TileLang 最常见的模板化方式（`block`、tile 尺寸
   等调优参数都从这里传入）。

### 1.3.3 查看生成的内核源码（强烈建议做一次）

```python
source = kernel.get_kernel_source()
print(source)
```

你会看到形如 `__global__ void add_kernel(...)` 的 CUDA 代码：一个 thread 处理多个
元素（循环展开/向量化）、访问模式经过合并（coalesced）、带边界保护。**把 TileLang
一行代码和生成的 CUDA 代码对照着看，是理解这个 DSL 最有效的方法**，也是面试时
"我研究过编译产物"的最好谈资。

### 1.3.4 性能基线

```python
profiler = kernel.get_profiler()
latency_ms = profiler.do_bench()          # 默认基于 CUDA event 多次计时取平均
print(f"vector add latency: {latency_ms:.3f} ms")

# 理论的 HBM 带宽利用率（以 A100 约 2TB/s 为例）
bytes_moved = 3 * N * 4                    # 读 A、读 B、写 C
bandwidth = bytes_moved / (latency_ms * 1e-3) / 1e12
print(f"achieved bandwidth: {bandwidth:.2f} TB/s")
```

这类带宽受限（memory-bound）内核的目标是**打满显存带宽**，而不是快在计算上。
面试常问："为什么 vector add 的优化空间很小？"——因为它每字节数据只做 1 次
加法，墙上时间基本由 HBM 带宽决定（roofline 模型中处于"内存墙"区域，见第 10 章）。

## 1.4 一个练习：改成 int32 并加边界

把 `dtype` 换成 `'int32'`，把 `N` 设成不是 `block` 整数倍的值（如 `N = 1000000`），
再跑一次验证。如果正确性报错，回忆 1.3.2 第 4 点——越界保护是自动的，但**逻辑
上自己的索引计算**（如 `gi`）仍要保证正确。

## 1.5 不同的编译入口：`tilelang.compile` 与 `@jit` 对比

| 方式                                                     | 适用                                                               | 示例             |
| -------------------------------------------------------- | ------------------------------------------------------------------ | ---------------- |
| `@tilelang.jit`                                        | 文档/原型/模板化内核；从实际张量推断目标与形状                     | 本教程大部分代码 |
| `tilelang.compile(func, out_idx=[...], target="cuda")` | 显式控制 target、指定输出参数（`out_idx`）让宿主自动分配输出张量 | 官方 matmul 教程 |

```python
# compile 风格：out_idx=[2] 表示第 3 个参数是输出，调用时不必传入
jit_kernel = tilelang.compile(func, out_idx=[2], target="cuda")
c = jit_kernel(a, b)   # 返回输出张量，而不是改写到第三个参数
```

`target` 的取值：`'auto'`（默认，从机器/张量推断）、`'cuda'`、`'hip'`、`'metal'`
等；执行后端（execution backend）有 `tvm_ffi` / `cython` / `nvrtc` / `torch`，
一般用 `'auto'` 即可。详见第 08 章。

## 1.6 本章小结

- 安装：`pip install tilelang`；无 GPU 可用云 GPU，语法部分可离线学习。
- 最小内核 = `@T.prim_func` + `with T.Kernel(...)` + 循环；`@jit` 负责编译与缓存。
- `T.Parallel` 让编译器替你分配线程与向量化；越界保护是自动的。
- 三个必做动作：`get_kernel_source()` 看生成代码、`do_bench()` 测性能、
  `torch.testing.assert_close` 验正确性。

## 面试考点（本章相关）

1. **TileLang 和纯 Python 的 `@T.prim_func` 有什么关系？**（AST/解析成 TIR，不是逐行执行）。
2. **`T.Kernel` 与 CUDA 的 grid/block 如何对应？**（`bx/by/bz` ↔ `blockIdx`；`threads` ↔ `blockDim`）。
3. **为什么 TileLang 能"替你分配线程"？**（循环 → 线程映射由编译器完成，见第 03、09 章）。
4. **`@jit` 装饰器是做什么的？**（JIT 编译 + 缓存；Python 参数在编译期烘焙进 IR）。
5. **vector add 是 memory-bound 还是 compute-bound？如何证明？**（roofline：实测带宽逼近峰值带宽）。
6. **`get_kernel_source()` 有什么用？**（理解抽象层、排查生成代码、面试展示深度）。
