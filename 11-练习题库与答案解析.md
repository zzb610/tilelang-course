# 第 11 章 练习题库与答案解析

这部分是全书配套的练习册。题目把正文里的概念重新放进代码、边界与性能情境中，检验你
在没有现成步骤时还能不能完成推理。自己推导答案时，理解不充分的地方才会露出来。每道题
都该留下答案、证据，以及你仍然拿不准的地方——后者常常比一个勉强正确的结论更有价值。

题目按“回忆—跟踪—修改—实现—优化—解释”递进。带 ⭐ 的题适合限时口述或白板练习；
代码题通常需要 CUDA GPU，概念题可以离线完成。优化题必须记录输入尺寸、基线、计时方法
和证据，边界题必须包含尾部尺寸。参考答案给的是评分要点，不是唯一实现。

---

## 第一部分：随正文推进的练习

这一部分按正文各章安排练习。每道题的难度与预计用时标在题号旁，可以先做入门题，再做核心
和进阶题。实际用时明显超过预计时间，通常说明要回看相应概念。

### 第 01 章 环境搭建与第一个内核

**E1.1**（入门，20min）完成第 01 章 vector add：跑通、`assert_close` 通过、用
`do_bench` 报告带宽利用率（3·N·4 字节 / 延迟）。

**E1.2**（进阶，30min）在 `block ∈ {128, 256, 512, 1024}` 下测延迟，并解释趋势。
⭐ **E1.3**（入门，5min）`@jit` 的缓存机制是什么？什么时候会重新编译？

### 第 02 章 核心语法

**E2.1**（入门，10min）读代码：

```python
# 读题时先标出网格 shape 和每个 block 的输出 tile shape。
with T.Kernel(T.ceildiv(N, 128), T.ceildiv(M, 64), threads=256) as (bx, by):
    # A/B/C 都是 [M,N]；每个 block 覆盖 [64,128] 的区域。
    for i, j in T.Parallel(64, 128):
        C[by*64+i, bx*128+j] = A[by*64+i, bx*128+j] + B[by*64+i, bx*128+j]
```
问：网格是几维？每 block 覆盖多大区域？`T.Parallel(64,128)` 的 8192 次赋值如何
被 256 线程执行（大致机制）？

⭐ **E2.2**（进阶，30min）用 TileLang 写 SAXPY：`C = a·A + B`（一维，a 为 Python
float 参数），并验证。

**E2.3**（进阶，20min）把 E2.2 的 dtype 换成 `bfloat16`，结果和 float32 比误差
如何？为什么？

### 第 03 章 内存层次与数据搬运

**E3.1**（入门，10min）为什么 GEMM 分块后访存从 O(MNK) 降下来？写出量级推导。
⭐ **E3.2**（进阶，45min）用 shared memory 实现「每行最大值」（`out[i] = max_j A[i,j]`），
先想清楚：谁并行谁串行、shared 里放什么。对比直接全局读的版本。
**E3.3**（思考，15min）`T.copy` 一个 `(BM, BK)` tile 进 shared：什么情况下会产生
bank conflict？提示：row-major 数组按行连续存放，warp 内线程通常映射到哪一维？

### 第 04 章 软件流水线与控制流

**E4.1**（入门，10min）`num_stages` 从 2 调到 4，共享内存消耗大约怎么变？为什么会
有限制？
⭐ **E4.2**（进阶，20min）解释下面注解下每轮执行的逻辑行为：

```python
# 这是一个手工流水线题目：A_shared/B_shared 是 tile，C_local 是累加 fragment。
for ko in T.Pipelined(num_tiles, stage=[0, 1], order=[1, 0]):
    # copy 生产下一轮输入，gemm 消费当前轮输入；stage/order 要按副作用语句对齐。
    T.copy(A[ko * BK], A_shared)
    T.gemm(A_shared, B_shared, C_local)
```
**E4.3**（挑战，60min）不用 `T.Pipelined`，改用 `T.async_copy` + `T.ptx_wait_group`
手写两趟流水线（prologue + steady + epilogue 思路）实现 GEMM 主循环，并对比性能。

### 第 05 章 GEMM 实战

⭐ **E5.1**（核心，90min）独立实现第 05 章 GEMM v1（分块 + Pipelined + T.gemm），
M=N=K=4096 fp16，报告 TFLOPS 并与 `torch.matmul`（cuBLAS）对比，误差容差 rtol=1e-2。
**E5.2**（进阶，45min）做 swizzle 开关实验：`annotate_layout(make_mma_swizzle_layout)`
开/关各测 5 次，报告中位数差异；再用 `T.use_swizzle(panel_size=10)` 重复。
**E5.3**（思考，15min）为什么 `T.gemm` 要求输入在 shared、输出在 fragment？如果输入
在 global 会怎样（从硬件/布局角度）？

### 第 06 章 FlashAttention

⭐ **E6.1**（核心，60min）脱稿写出在线 softmax 三公式，并手写一个 5 行 3 块（每块
2 元素）的数字例子验证 `l_new` 与直接 softmax 全行 sum 一致。
**E6.2**（进阶，60min）把第 06 章 FA 代码改造成非因果版本（去掉 causal 分支），
对 `seq_len=2048, dim=64` 验证，并与 einsum 参考对比。
**E6.3**（思考，15min）为什么最后只做一次 `acc_o /= logsum[i]` 而不是每轮除？

### 第 07 章 高级指令与布局

**E7.1**（入门，15min）画出 8 个 bank（想象 32 个）下「步长 4 访问」与「步长 3 访问」
的冲突情况，说明为什么奇数步长通常天然无冲突。
**E7.2**（进阶，45min）用 `T.warp_reduce_sum` 或 shfl 手写 block-level sum：
每 block 128 线程归约 128 个值到 1 个（先 warp 内归约，再跨 warp 用 shared）。
**E7.3**（读代码，10min）

```python
# value 是一个 warp 内标量；shfl_down 返回同一 warp 中偏移 delta 的 lane 值。
x = T.shfl_down(value, delta)
```
问：`delta=8` 时 lane 4 拿到谁的值？lane 28 呢（边界行为）？

### 第 08 章 自动调优与工程化

⭐ **E8.1**（进阶，60min）给第 05 章 GEMM 加上 `@tilelang.autotune`（configs 覆盖
BM/BN/BK/stages/threads），用 `set_autotune_inputs` + `ref_prog` 校验，报告最优配置
与对应 TFLOPS。
**E8.2**（入门，15min）写一个会**剔除非法配置**的 configs 函数：例如共享内存超
48KB 的 (BM,BN,BK,stages) 组合直接不 yield。
**E8.3**（概念，10min）autotune 磁盘缓存的键包含哪些内容？为什么配置变了会重新
编译？

### 第 09 章 编译流程与调试

**E9.1**（进阶，30min）把第 05 章 GEMM 存成脚本，`TL_LOWER_TRACE=html` 跑一遍，
找到 `T.copy` 变为 `cp.async`/屏障的那个 pass，截图说明。
**E9.2**（挑战，60min）故意写一个「结果错」的 kernel（如索引算错、忘记 clear），
用 `T.print` + 参考实现逐块对比，定位出 bug 行。记录排查过程。
**E9.3**（概念，10min）`LegalizeSafeMemoryAccess` 对「证明安全」的访问会怎样？为什么
需要「证明」而不是永远插入 if？

### 第 10 章 面试冲刺

⭐ **E10.1**（核心，15min 口述）对着手机录音，15 分钟内讲完「高性能 GEMM 设计」，用
第 10 章 10.7 框架，回放检查是否包含：bound 判断、数据流、流水线、细节、验证。
**E10.2**（进阶，30min）找一位朋友扮演面试官，问 10.3-10.4 中任意 8 题，互相点评。
**E10.3**（核心，30min）写一个 5 分钟版本的项目自我介绍（模板见 10.6），包含一个
具体数字（TFLOPS/加速比）与一项复盘结论。

### 第 13 章 分组 GEMM

**E13.1**（入门，15min）给定 `M=[130,32,256]`、`N=256`、`BM=BN=128`，写出每组
tile 数、前缀和，并判断线性 block 5 所属的 group 和组内坐标。

**E13.2**（进阶，30min）实现 `group_sizes → row_offsets → padded_offsets →
group_idx_for_block` 的 host 侧元数据构造，并覆盖空组和尾部组。

⭐ **E13.3**（核心，60min）在 `A` 已按 `BM` 补零的前提下，补齐第 13 章 grouped GEMM
骨架的 host 包装，用逐组 `torch.mm` 作为参考实现，验证至少 3 组输入。

**E13.4**（挑战，90min）比较均匀分组、倾斜分组和按 M 分桶三种方案，分别报告
kernel-only、end-to-end、useful TFLOPS、padding 利用率和 metadata 开销。

### 第 14 章 收官项目

**E14.1**（入门，30min）从官方 examples 选 3 个候选算子，为每个候选写出目标硬件、
输入 shape 分布、参考实现、现有库路径和一个最难的边界，最后只保留 1 个。

**E14.2**（进阶，60min）为选定算子建立 shape/dtype 测试矩阵，至少包含整除、尾部、
极小和真实 workload 四类输入；给每类输入说明容差和预期 fallback。

⭐ **E14.3**（核心，1～2 周）按第 14 章验收表提交一个真实算子：可复现环境、正确基线、
至少两个单变量优化实验、库对比、生成代码或 profiler 证据、dispatch 范围和失败复盘。

**E14.4**（资料审阅，30min）选择一篇社区 TileLang 教程，列出其中 3 条可核对结论。
分别用官方文档、当前源码/示例或本地最小实验核对，并说明哪些结论不能跨版本或硬件外推。

**E14.5**（评估方法，45min）给 E14.3 的最终内核补上第 14 章 14.6.1 的两道筛查：
① 计算目标 shape 分布上的相对库效率；② 按 bound 类型取 W（字节数或 FLOPs）算
roofline 参照指标 `ρ = W/(t·P)`；③ 报告初始版到优化版的 cliff。若 `ρ ≈ 0.5` 附近两道筛查
给出矛盾结论（例如相对效率尚可但 ρ 低，或相反），写出你的判读与下一步。

---

## 第二部分：综合问题

逐章练习之后，把知识串成整体来做模拟。下面五道题混合了前九章的知识点，每道都带考官追问，
用来模拟真实面试里"答完还不够，还要被连续追问"的节奏。从这一部分开始，你练的不只是知识
点，更是「被打断之后还能把思路接回来」的本事。

**S1 ⭐ 手写 GEMM**：给 10 分钟白板/编辑器中写出 TileLang GEMM（或 CUDA 伪代码），
考官追问：① 为什么 fp32 累加？② 共享内存 48KB 时 (BM,BN,BK,stages) 怎么搭配合法？
③ 如果 M=1024, K=10（瘦长形状），你会改什么？

**S2 ⭐ FlashAttention 推导**：① 推导在线 softmax 三公式；② 说出 FAQ 常问的
「为什么不需要存 P」；③ 追问：causal 时 block_M=128、block_N=64、seq=1000 的
K 循环次数怎么算？

**S3 ⭐ bank conflict 与 swizzle**：① 画图解释；② 追问：给定元素大小、bank 模型和
线程访问坐标，怎样推导冲突并选择 padding/swizzle？③ 追问：光栅化 swizzle 和
bank swizzle 的区别？

**S4 软件流水线设计**：给定 producer（global→shared 3ms）与 consumer（计算
2ms/轮），串行 5 轮共多少？3 级流水线呢？画时序图。

**S5 调优方法论**：你的 FA 在 RTX 4090 上比 PyTorch SDPA 或已验证的 Attention 库路径
慢 20%，如何一步步归因？
（要求答出：bound 判断 → 实测带宽/TFLOPS → 生成源码审查 → Nsight stall 分析 →
单项开关实验的顺序。）

---

## 第三部分：参考答案与推理线索

做完题再对照答案，更容易发现自己哪里没想透。这里的答案给的是必要要点，不是唯一解。
请先独立完成题目，再把漏掉的要点补进记录。

### E1.3
`@jit` 首次调用编译并缓存（进程内 + 磁盘 `~/.tilelang/cache`）；缓存键含版本、
函数源码、闭包自由变量、参数等。**改动函数体/参数/版本都会触发重编译**；
`TILELANG_DISABLE_CACHE=1` 强制不缓存。

### E2.1
网格 2 维：`(ceildiv(N,128), ceildiv(M,64))`；每 block 覆盖 64×128 的 tile；
8192 次赋值可由编译器映射到 256 线程，具体每线程工作量、是否向量化和访问是否合并
要由目标、布局和生成代码确认；也可能重排循环（外并行内串行）以改善访存效率。

### E2.3
bfloat16 尾数 8 位（fp32 23 位），表示范围大但精度低：加法结果误差更大；
若做累加（如 GEMM），误差随 K 累积，标准做法是**输入 bf16、累加 fp32**。

### E3.1
朴素：每个输出读 O(K) 个全局值，总 O(MNK)。分块后每个 block-stage 读
(BM·BK + BK·BN) 个值供 BM·BN 输出复用：总读 ≈
MNK·(BM·BK+BK·BN)/(BM·BN·BK)，另有 O(MN) 的输出写回。
设 BM=BN=BK=T，则复用后 K 维每个值只被读 O(M·N) 份拷贝共
MN·(2T)/T² = 2MN/T 次，而朴素是 MNK 次：**访存降 K/T 倍**。K 越大、T 越大越省。

### E3.2
方案：每 block 处理一行（网格 = M），行读进共享内存后串行扫（或并行归约）。
答出：**数据流 global→shared→register**、以及「并行在哪：block 间按行并行，
行内可做 tree reduce」即为合格；给出对比：全局直读版每行被重复读多次。

### E3.3
row-major 矩阵 `(BM, BK)` 拷贝：warp 线程通常沿**最后一维（列/K）**连续映射，
连续 32 元素分布在 32 个 bank → 无冲突。**当访问模式变成「每线程跳 32 元素」或
按列取（列主序布局）**时冲突。所以行主序 tile 拷贝天然安全，转置/跨步访问才需要
swizzle。

### E4.1
每多一级，A/B 各多一份 shared tile：`2·BM·BK + 2·BK·BN` 级别的增量（还要算
double buffer 结构）。限制：共享内存上限（默认 48KB，可动态申请更大）、以及
stages 太多导致同步/启动开销反超收益。经验值 2-4。

### E4.2
两个调度语句：copy → stage0/order1，gemm → stage1/order0。发射顺序先 gemm
（order0）后 copy（order1）？——不，`order` 控制**发射顺序**：order0 的 gemm 先
发，但 gemm 属于 stage1（消费），copy 属于 stage0（生产），流水线会把 copy 提前
到前一迭代执行。**结论**：这是「本轮消费者先发、下轮生产者后发」的时序，效果是
拷贝与计算重叠。答出「producer stage ≤ consumer stage，跨轮重叠」即满分。

### E4.3
结构：prologue 发 `ko=0,1` 的 `async_copy`；steady 每轮 `ptx_wait_group(0)` 等
本轮、发下一轮、`T.gemm` 算本轮；epilogue 处理残留。注意 `wait_group` 的号与
group 计数一致、以及**共享屏障时机**（消费前）。不要预设手写版本一定更快；应与
`T.Pipelined` 使用相同输入和计时条件比较，并检查屏障、向量宽度、group 管理和寄存器/
共享内存开销。

### E5.3
`T.gemm` 需要 shared 输入：Tensor Core 直接消费的片段需要特定布局（swizzle/
ldmatrix 友好），且 shared 是 block 级共享——数据要被 128 线程按 MMA 布局取用，
fragment（寄存器）是每线程私有的，全局读每个线程取不到「别人那部分」。输出必须
fragment：累加器要留在寄存器里跨 K 轮累积、且 tiling 时每线程只持有切片。若输入
在 global：布局不满足、无共享复用、访存量过大 → 编译器拒绝或性能明显下降。

### E6.1
数字例：行 logits = [1, 2, 3, 4]，分两块 [1,2] 与 [3,4]：
块1: m1=2, l1=e^-1+e^0≈1.368；块2: m2=4, l2=e^-1+e^0≈1.368。
合并：m_new=4；l_new=1.368·e^(2−4)+1.368·e^(4−4)=1.368·0.1353+1.368≈1.553。
直接算：Σe^(x−4)=e^-3+e^-2+e^-1+e^0≈0.0498+0.1353+0.3679+1=1.553 ✓。

### E6.3
因为**每轮都归一**反而会丢精度且多开销：O 的每轮缩放已由 `scores_scale` 跟踪
（旧行乘修正因子），分母 `logsum` 也按同一因子演进，二者恰好同源；最后一次性
除以 logsum 就是恢复整个计算过程中的隐含归一化。online softmax 的优势在于：
**归一化只在最后做一次**。

### E7.2
思路：每 warp 内 `T.warp_reduce_sum(thread 值)`（5 步 shuffle）；warp 0..3 各得
一个部分和，由 lane0 写入 shared[4]；`__syncthreads`；warp0 再把 4 个值相加。
要点：`shfl_down` 的 delta 序列 16,8,4,2,1 需 5 步；跨 warp 必须经过共享内存 +
屏障（寄存器不可跨线程）。

### E7.3
`shfl_down(value, delta=8)`：lane 得到 `value[src]`，其中 `src = lane + 8`（掩码内）。
lane 4 ← lane 12；lane 28 ← lane 36，但 36 ≥ 32，**该 lane 得到自己（或未定义）值**
——CUDA 语义里超出 active mask 的 lane 返回自身值（若 mask 不含则未定义）。
答案要点：换位方向 + 越界行为（通常保留原值/未定义）。

### E8.2
要点：①按 BM·BK + BK·BN（×stages 必要时）估算 shared 字节，>48KB（或动态上限）
跳过；②BM/BN/BK 与 threads 的整除关系（如总线程数 ≤ BM·BN/BK 的可用性依赖
布局）与寄存器压力估算；③ 非 2 的幂 tile 与向量宽度 16B 的关系。写出一个
`yield`/`continue` 过滤的生成器即可。

### E8.3
键 = tilelang 版本 + 函数源码 + 闭包自由变量 + configs 列表 + compile args +
profile args。任何一项变化（版本升级、改了一行源码、config 集合不同）都会导致
新键 → 重新编译/调优。**这就是为什么「固化最佳配置」要存下来供 CI 复用**。

### E9.3
对能被静态证明不越界的访问，pass 可以不插入额外 guard，避免无谓分支；只有**可能**
越界的访问才需要保护。这依赖编译期范围分析，所以叫「证明安全」。但对参与 GEMM、
归约或 softmax 的无效输入，仍要由程序明确选择 guard、padding 或零填充的数值语义。

### E13.1

`N=256、BN=128`，所以每组有 2 个 N 方向 tile。M 方向 tile 数为 `[2,1,2]`，
每组 tile 数为 `[4,2,4]`，前缀和为 `[0,4,6,10]`。block 5 属于 group 1，组内 tile
编号是 `5-4=1`，因此 `by=0、bx=1`。

### E13.2

`row_offsets` 是 `group_sizes` 的前缀和，描述 packed A/C 的真实行区间；
`padded_offsets` 是 `ceildiv(M_g,BM)*BM` 的前缀和，描述物理 padded 行区间；
`group_idx_for_block` 按每组的 M 方向 tile 数重复 group id。空组的 tile 数为 0，
不生成有效 block；若必须保留空组工作项，则 kernel 需要在读写前显式跳过。

### E13.3

先用逐组 `torch.mm` 建立参考，再验证 `G=1`、一个 M 尾部组和一个包含空组的输入。
若 `A` 采用补齐布局，补齐行必须填 0；输出仍按真实 `row_offsets` 写回，并用
`valid_m` 保护最后一个 tile。性能报告不能只写 kernel 延迟，还要说明 pack、metadata
和 scatter 是否计入 end-to-end 口径。

### S1
回答链（详见 10.4 Q25）。追问①：精度 + Tensor Core 要求；追问②：
`BM·BN·4B(fp32 acc) + stages·(BM·BK+BK·BN)·2B(fp16)` ≤ 48KB，给出合法组合并
说明 trade-off（如 128×128×32 + 3 级 ≈ 24+ 24KB╳若超则降 stages 或 BK）；
追问③：M=1024, K=10 → 计算强度低，很可能 memory-bound → 改小 BK、去掉流水线
深度的必要性降低、考虑把 K 合并进 tile 单趟算完（甚至不做 shared 缓冲，直接
fragment 累加），或按带宽预算设计。

### S2
公式见 10.4 Q26。追问③：causal 时每个 query 块只看到前 (bx+1)·block_M 的 kv；
block_M=128：loop_range = min(ceildiv(1000,64), ceildiv((bx+1)·128,64))，
bx=0 时 = min(16, 2) = 2；bx=7 时 = min(16,16)=16。答出「用 T.min 截断」即可。

### S3
提示：掩码选低位（如 `chunk_idx ^ (row & 7)`，其中 `chunk_idx` 是 16 字节块的
序号）是因为**冲突来自同一 bank 窗口内的低位地址**；在 16B（128 位）块级别上
XOR 可以打散 4-bank 子窗口（16B 覆盖 4 个 bank，32 线程访问 32 个 16B 块 →
碰撞面缩小），同时**保持 16B 向量对齐，ld.shared.v4 仍可用**；若直接 XOR 整个
字节地址（`addr ^ row`）或以 4 字节粒度 XOR，会破坏对齐/向量化。

### S4
串行：5×(3+2)=25ms。流水线 3 级：理想 3（prologue）+ 2×5（steady 每轮 max
(拷贝,计算)）= 13ms；更精确画 prologue 填充 + steady + epilogue 梯形图。要点：
**关键路径 = prologue + max(生产,消费)×n + epilogue**。

### S5
按第 09 章决策树：① 先用算术强度和 profiler 判断 bound 类型，不预设所有 FA 都是
memory-bound；② 实测显存带宽利用率与 TFLOPS 定位差距大小；③ 审生成源码：Q/K/V 拷贝是否
cp.async/TMA、屏障是否过密、acc 是否 fp32；④ Nsight Compute：看 warp 停在
`long scoreboard`（等内存）还是 `barrier` 还是 `math`；⑤ 单项开关（块尺寸、
stages、swizzle、rasterize）量化；⑥ 收敛。**答出「先分 memory 还是 compute 再
对症」即满分。**

---

## 本章回顾

练习的价值不只在完成题目，还在运行数据、查看源码和撰写报告。保留实验证据和推理过程，
你才判断得出自己是不是真懂，其中画数据流图和排查边界错误尤其重要。模拟题 S1–S5 覆盖了
面试的四大题型，考前可各做两遍，第二遍限时完成，看看压力下还能不能把思路讲完整。参考答案
给的是「要点清单」而非标准答案；真到面试时，用第 10 章 10.7 的框架把要点展开成完整表达，
才是得分的关键。
