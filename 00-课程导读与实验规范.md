# 课程导读与实验规范

这套教程的主线不是「背 API」，而是先建立执行模型，再训练一条可以迁移到 CUDA、Triton
或其他 GPU DSL 的工作流：

```text
定义问题 → 写最小正确基线 → 构造参考实现 → 测正确性 → 测延迟/带宽
→ 看生成代码与 profiler → 只改一个变量 → 记录证据 → 固化配置
```

**阅读路线：**先确认课程目标和自己的前置知识，再完成诊断题；随后读懂三种代码块的边界，
最后复制实验记录模板开始第 01 章。诊断题答不上来时，先补对应概念，不必把整套课程从头重读。

## 1. 学完后应具备的能力

完成主线（第 01～09 章）后，学习者应能：

- 解释 host/device、grid/block/thread、global/shared/fragment 的关系；
- 写出带边界处理、误差容忍度和错误检查的 TileLang 内核；
- 用 `get_kernel_source()`、`do_bench()` 和 Nsight 工具观察编译产物与瓶颈；
- 从 elementwise、归约、GEMM、Attention 中识别数据复用、同步和流水线问题；
- 用测量结果决定是改 tile、布局、流水线、线程数，还是直接使用库；
- 为内核补齐 edge shape 测试、基线对比、性能报告和版本说明。

第 10～12 章是复习与迁移：第 11 章用于逐章练习，第 10 章用于面试表达，第 12 章只作为查表工具，不替代主线学习。
全书的写作和术语约定见 [WRITING_GUIDE.md](WRITING_GUIDE.md)。

## 2. 适用人群与默认假设

- 已会 Python，知道数组、矩阵乘和基本浮点数概念；
- 最好会读一点 C/C++，但不要求先学完整 CUDA C++；
- 目标硬件默认为 NVIDIA CUDA GPU；
- macOS/Metal 只承担语法、elementwise 和有限 smoke test，不把 Metal 的结果当作 CUDA 性能结论；
- 课程不承诺所有版本敏感 API 在所有后端都可用，遇到版本差异先看 `resources.md` 和官方示例。

## 3. 开始前的诊断

不需要把诊断题当作考试。若下面有 2 项以上答不上来，先完成对应补课，再进入主线。

1. 给出 row-major 的 `A[M, N]` 中元素 `(i, j)` 的线性偏移；
2. 解释为什么 `ceildiv(N, block)` 能覆盖尾部 tile；
3. 说出「读 global、写 global」的 vector add 更可能受算力还是带宽限制，并说明如何验证；
4. 解释为什么 GPU 内核需要 bounds check，以及为什么「编译器插入 guard」不等于「越界读会自动变成 0」；
5. 说出 warmup、同步和重复计时分别解决什么问题；
6. 能在当前机器上打印 Python、PyTorch、TileLang 版本，并判断 CUDA/MPS 是否可用。

## 4. 代码块的三种等级

为避免复制代码后才发现缺少上下文，全文按下面的约定阅读：

| 标记 | 含义 | 学习者动作 |
|---|---|---|
| `可运行示例` | 包含必要 import、输入、调用和校验 | 复制到独立脚本运行；优先用小尺寸验证 |
| `内核骨架` | 重点展示 TileLang 结构，可能省略 host 或边界细节 | 阅读、补全，并完成本章 checkpoint |
| `伪代码/面试模板` | 用于解释调度、时序或设计思路 | 不要直接当作可执行程序 |

原有章节中的长代码若没有明确标记，默认按「内核骨架」理解；改进后的关键示例会明确说明运行前提。

## 5. 每个实验都要留下的证据

至少覆盖 3 组尺寸：一个整除 tile 的尺寸、一个尾部不整除的尺寸、一个很小的尺寸（如 1 或 7）。性能实验再补一组实际目标尺寸。

正确性至少包含：

- 与 PyTorch/NumPy 参考实现比较；
- 明确 `rtol`、`atol`，并说明为什么这样设置；
- 检查输出是否包含 NaN/Inf；
- 失败时缩小到最小复现形状。

性能记录至少包含：

```text
GPU / 驱动 / CUDA 或 ROCm / TileLang / PyTorch：
输入形状与 dtype：
warmup / rep / 计时 backend：
基线延迟：
修改后的延迟：
带宽或 TFLOPS：
生成代码/Profiler 观察：
瓶颈假设：
只改变了什么：
结论与下一步：
```

性能数字只对「硬件、版本、输入、计时方法」这个组合负责。不要把教程中的 TFLOPS 当成验收答案，也不要只报告 speedup 而不报告正确性和测量条件。

## 6. 推荐学习节奏

### 最小可用路径（2 周）

00 → 01 → 02 → 03 → 05 → 06 → 11 中对应练习 → 10 → 12。

### 系统路径（6～8 周）

每周完成 1～2 章，顺序为 01 → 02 → 03 → 04 → 05 → 06 → 07 → 08 → 09；每章先做正文实验，再做第 11 章对应练习，最后写一页实验记录。

### 研究/工程路径

完成系统路径后，阅读 `resources.md` 中的官方示例，选一个算子做小型 capstone：基线、正确性、优化、profile、库对比、版本/硬件 caveat 和复盘缺一不可。

## 7. 通过门槛

| 阶段 | 通过条件 |
|---|---|
| 正确性 | 能处理小尺寸和尾部尺寸，失败时能定位到索引、边界或数值误差 |
| 性能 | 能在优化前先测量，并用一个可观察指标支持瓶颈判断 |
| 高级特性 | 能说明适用的 GPU 架构、同步要求和 fallback，不把单一 GPU 的技巧当通用规律 |
| 工程化 | 能复现环境、固定输入、保留基线，并把最佳配置与测试一起归档 |
