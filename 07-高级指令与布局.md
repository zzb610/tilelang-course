# 第 07 章 高级指令与布局：mma、TMA、warp 原语

> **本章目标**：从"会用 TileLang"升级到"懂 TileLang 底下在干什么"：布局推断、
> swizzle 消除 bank conflict、Tensor Core 指令、显式异步拷贝（cp.async/TMA）、
> warp 级原语。这一章的知识是"专家"与"会用"的分水岭，也是深度面试的题库。

## 7.1 布局推断（Layout Inference）：TileLang 的魔法核心

你写 `C_f = T.alloc_fragment((128, 128), 'float32')`，128×128 = 16384 个元素，
128 个线程，那么**每个线程实际持有 128 个元素，但具体是哪 128 个？**——由
**布局推断**决定。编译器根据所有使用该 buffer 的操作（GEMM 的 MMA 片段、`T.copy`
的访存模式、`T.Parallel` 循环……）反向求解一个一致的每线程数据分配。

意义（面试要会讲）：

- 写 tile 级代码时**不必手动管理线程内数据摆放**——只管语义，编译器管布局；
- 提速：布局推断会尝试让访问模式 coalesced / 无 bank conflict；
- 代价：当推断不出唯一布局或推断结果不佳时，需要你手动干预（`T.annotate_layout`）。

## 7.2 共享内存 bank conflict 与 swizzle（重点）

### 7.2.1 什么是 bank conflict

共享内存被分成 **32 个 bank**（每 bank 4 字节，一个周期可同时服务 32 个地址）。
warp 内 32 个线程同时访问 `addr_i`，若 `addr_i % 32`（地址按 4B 对齐时）出现重复，
bank 冲突发生，访问被**串行化**（冲突 n 路 → 慢 n 倍）。

```text
访问步长 32（每个线程跳 32 个元素，即 128B）：
thread 0 → bank 0, thread 1 → bank 0, ...   // 全部撞 bank 0 → 32 路冲突，最坏情况
```

典型高危形态：按列访问行主序矩阵；步长为 2 的幂；MMA 共享内存片段特定位型。

### 7.2.2 swizzle：XOR 打散

思想：把"物理地址 = 逻辑地址"改成"物理地址 = 逻辑地址 XOR 某个按行变化的掩码"，
让原本同 bank 的一列被摊到不同 bank。经典做法（CUTLASS/TileLang 的
`make_mma_swizzle_layout`）：

```python
from tilelang.cuda.intrinsics import make_mma_swizzle_layout

T.annotate_layout({
    A_shared: make_mma_swizzle_layout(A_shared),
    B_shared: make_mma_swizzle_layout(B_shared),
})
```

官方 `matmul.md` 里这两行就是 GEMM 性能的关键开关之一。直觉图：

```text
原始： row0: [0][1][2]…[31]   row1: [32][33]…  → 列访问撞 bank
swizzle：row1 整体 XOR 偏移后：col 访问落到不同 bank
```

## 7.3 光栅化 swizzle：`T.use_swizzle`

注意别混淆——`T.use_swizzle` 不是内存布局，而是 **block 调度顺序**（rasterization）：

```python
with T.Kernel(...) as (bx, by):
    T.use_swizzle(panel_size=10, enable=True)
```

- 默认 block 按 (by, bx) 字典序调度，同一时刻执行的一批 block 落在矩阵的**同一条
  条带**上，L2 预取/复用率差；
- 光栅化把调度改成**对角线顺序**（每 panel 内蛇形），让并行执行的 block 访问的
  数据在 L2 上更"聚拢"，典型收益几个百分点；
- `panel_size` 控制一个"面板"的大小，越大越接近对角线模式。

## 7.4 `T.gemm` 深入

```python
T.gemm(A_s, B_s, C_f, transpose_B=True, policy=T.GemmWarpPolicy.FullRow)
```

| 参数 | 作用 |
|---|---|
| `transpose_A / transpose_B` | 输入是否需要转置（FA 里 K 需要转置） |
| `policy=T.GemmWarpPolicy.FullRow` | 把 warp 划分到输出片段的方式（每 warp 独占整行片段，减少共享内存带宽压力） |
| `A_s/B_s` 必需在 shared，`C_f` 是 fragment 累加器 | 布局不匹配时编译期报错（这正是布局推断的用武之地） |

底层映射：NVIDIA 上降级为 `mma.sync`（sm_80+）/ `wmma`（sm_70）/ `wgmma`（sm_90a，
warpgroup MMA）等；AMD 上落到 HIP 的 tensormap/combination 或
`__builtin_amdgcn_wmma_*`。**不用记指令细节，但要能说出"T.gemm 直通 Tensor Core，
并且自动处理数据布局"**。

衍生：`T.gemm_sp`（2:4 结构化稀疏，配合稀疏元数据）、FP8 GEMM（`examples/
gemm_fp8/`）。

## 7.5 显式异步拷贝族

| API | 硬件 | 语义 | 手动同步 |
|---|---|---|---|
| `T.copy` | ld.global / ldmatrix / cp.async / TMA（编译器选） | 同步 | 无 |
| `T.async_copy` | `cp.async` | 显式异步 | **必须** `T.ptx_wait_group(...)` |
| `T.tma_copy` | `cp.async.bulk`（TMA） | 显式异步 | mbarrier / `tma_store_wait` 等 |

```python
# cp.async 手动流水
T.async_copy(A[by * BM, ko * BK], A_s)   # 发出拷贝 + commit group
# …做与 A_s 无关的计算…
T.ptx_wait_group(0)                       # 等 group 0 全部完成
T.gemm(A_s, B_s, C_f)                     # 之后读 A_s 安全（屏障自动插入）

# TMA（Hopper+）：描述符 + bulk copy，绕过寄存器、硬件搬运
T.create_tma_descriptor(...) ; T.tma_load(...) ; T.mbarrier_arrive(...)
```

`T.async_copy` 是**严格**的：无法降级成 cp.async 就编译失败，绝不静默回退成同步
拷贝——这是刻意的，防止你误以为"异步"其实是同步的。

其它：`T.transpose(src, dst)`（共享内存转置）、`T.c2d_im2col`（卷积 im2col 便捷）。

## 7.6 同步、mbarrier 与内存序

```python
T.sync_threads()                    # __syncthreads（可指定 barrier id/arrive_count）
T.sync_warp()                       # __syncwarp
T.sync_grid()                       # 协作式 grid 同步（需 cooperative launch）

# mbarrier（Hopper+ TMA 协作常用）
b = T.alloc_barrier(arrive_count)
T.mbarrier_arrive(b)
T.mbarrier_wait_parity(b, parity)
T.mbarrier_expect_tx(b, ...)        # 声明期望的传入字节数
T.fence_proxy_async()               # async proxy fence
```

同步黄金法则（面试点）：**shared 内存的写者必须先 `__syncthreads` 再做跨线程读**；
`cp.async/TMA` 的完成信号（wait_group / mbarrier）管的是"数据到没到"，屏障管的是
"线程间可见性"，两者不可互相替代。

## 7.7 warp 原语速查（CUDA 语义，HIP 有差异）

```python
T.get_lane_idx()            # 线程在 warp 内的 lane id
T.get_warp_idx()            # warp id

# shuffle：warp 内换数据，零共享内存开销
T.shfl_sync(value, src_lane)     # 广播
T.shfl_down(value, delta)        # 向下移 delta
T.shfl_xor(value, delta)         # XOR 换位（warp 归约经典操作）
T.shfl_up(value, delta)

# 投票/选择
T.ballot_sync(pred)          # 返回 mask：哪些 lane 的 pred 为真
T.activemask()               # 当前活跃 lane mask
T.any_sync(pred); T.all_sync(pred)
T.match_any_sync(value)      # 值相同的 lane mask（sm_70+）
T.syncthreads_count(pred)    # 同步并统计

# 归约（自动用 shuffle + 展开）
T.warp_reduce_sum(x); T.warp_reduce_max(x) ...
```

| 原语 | 典型用途 |
|---|---|
| `shfl_down` | 一眼看穿的归约模式（步长 16,8,4,2,1） |
| `ballot` | 自适应掩码、稀疏索引收集 |
| `any/all_sync` | 跨线程终止条件（如 `if T.any_sync(done): break`） |
| `syncthreads_count` | 统计多少个线程越界，决定是否走边界路径 |

注意（HIP 差异）：`any/all` 忽略 mask 直接调 `__any/__all`；`ballot` 原生于
64 线程 wavefront 返回 uint64；`match_*` 在 HIP 上**不可用**（会 codegen 失败）。

## 7.8 原子操作

```python
T.atomic_add(dst, value, memory_order=None, return_prev=False)
T.atomic_max(dst, value, memory_order=None, return_prev=False)
T.atomic_min(dst, value, memory_order=None, return_prev=False)
T.atomic_addx2(dst, value); T.atomic_addx4(dst, value)   # 向量化原子
T.atomic_load(dst); T.atomic_store(dst, value)
T.loop_break()                 # 从循环里跳出（intrinsic 形式）
```

用途：多 block 累加同一输出（如 split-K 合并）、直方图、前缀和定址、稀疏写入
（`return_prev` 做唯一序号分配）。

## 7.9 其它专家级工具

```python
T.dp4a(A, B, C)          # 4 元素 int8 点积累加（DP4A 指令，int8 GEMM 核心）
T.clamp(x, lo, hi)
T.device_assert(cond, msg='...')          # 设备端断言（CUDA）
T.annotate_l2_hit_ratio(buf, ratio)       # 缓存行为提示

# 寄存器压力控制（影响占用率）
T.set_max_nreg(count, is_inc)
T.inc_max_nreg(n); T.dec_max_nreg(n)
T.annotate_producer_reg_dealloc(n=24)     # 生产者提早释放寄存器
T.annotate_consumer_reg_alloc(n=240)
```

## 7.10 Blackwell 前瞻（一句话）

- `T.alloc_tmem`：Tensor Memory（TMEM）缓冲（`tcgen05` 指令）；
- warpgroup 原语：`T.warpgroup_arrive() / commit_batch() / wait(num_mma)`、
  `T.wait_wgmma(id)`（Hopper wgmma）；
- 这些接口面向 sm_90a/sm_100+，**面试提到"了解存在 + 知道为谁服务"即可**，细节
  以官方 repo 为准、且随版本演进。

## 7.11 本章小结

- 布局推断让 tile 级代码免于手动线程分配；`annotate_layout` + `make_mma_swizzle_layout` 是性能开关。
- bank conflict（地址%32 撞 bank）用 XOR swizzle 消除；`T.use_swizzle` 是 block 调度优化，别混。
- `T.copy` 同步、`T.async_copy`（cp.async）必须手动 wait、`T.tma_copy`（TMA）走 mbarrier。
- warp 原语（shfl/ballot 等）解决"warp 内通信"；原子解决"跨 block 累加"。

## 面试考点（本章相关）

1. **画图解释 bank conflict 与 swizzle 原理**（注意 32 bank、4B 对齐、XOR 掩码）。
2. **布局推断是什么？什么时候会失败/需要干预？**（多约束冲突、显式喂 MMA 布局）。
3. **cp.async 与 TMA 的区别？**（SM 内异步拷贝 vs 硬件 DMA 引擎 + mbarrier；TMA 省寄存器、支持 bulk/多目标）。
4. **`T.gemm` 底下是什么？**（Tensor Core 指令 + 布局转换；wmma/mma/wgmma）。
5. **shfl_down 做 warp 归约的代码与复杂度？**（log(32)=5 步）。
6. **同步 vs 等待的区别？**（wait_group 管数据到达；barrier 管可见性）。
7. **`T.use_swizzle` 优化的是什么？**（L2 局部性、调度顺序，不是 bank）。