# 教材写作约定

这份约定服务于本仓库的所有章节。它不要求各章长成同一个样子，而是规定一部技术教材
应当怎样展开问题、建立概念、使用代码证据，并把前后章节连成一条可持续阅读的思路。

本仓库写的是一部可以动手验证的技术教材，不是产品宣传页，也不是 API 词典。正文首先
帮助读者理解对象、关系和因果；命令、清单与检查表只在确实需要操作或查阅时出现。

## 0. 教程的不可改动边界

- 保留代码字面量、API 名称、命令、路径、版本、数值、限制和引用；不要为了句子顺畅自行补事实。
- 版本敏感的行为写成「当前版本/当前后端下的观察」，不要写成所有环境都成立的规则。
- 性能结论必须绑定硬件、软件版本、输入形状、dtype、warmup、rep 和计时方法。
- 代码块、公式、日志和表格是证据，不要用泛泛的总结句替代它们。
- 现有中文文件名和链接属于项目约定，不按通用文档规范强行重命名。

## 1. 全书共享一条思想线，不共享一套章节模板

全书从“写出正确内核”出发，逐步追问数据在哪里、等待从何而来、算子怎样利用硬件，最后
进入测量、调试和工程交付。每章都应承接前章尚未解决的问题，并在结尾自然提出下一章需要
处理的新矛盾。

新概念通常沿着下面的认识过程展开，但不把这些步骤机械地写成同名小标题：

1. **先提出问题**：它解决什么具体问题，不使用它会出现什么现象；
2. **再建立模型**：说明输入、输出、作用域、依赖和执行顺序；
3. **再给最小代码**：只引入当前概念需要的 API；
4. **再解释观察点**：说明代码如何对应模型，以及读者运行后应该看到什么；
5. **再做实验**：先验证正确性，再测性能或观察生成代码；
6. **最后处理边界**：说明尾部尺寸、失败路径、版本差异和下一章的连接点；
7. **留下可验证结果**：在章末实验中提交代码、测试、图、表或排障记录，而不只是复述定义。

如果一个例子同时引入多个新概念，应先给出简化版，再给出综合版，并明确两者的差别。
如果必须提前使用尚未讲解的概念，应在代码前用一句话说明它的作用，不让读者靠猜。

一节只推进一个主要认识。可以使用“问题—模型—代码—验证—边界”作为作者的内部检查，
但读者看到的应是连贯论述，而不是反复出现的流程标签。

章节形态可以不同：概念章适合从反例和模型展开，算子章适合围绕同一实现逐版演进，调试章
适合从故障现场倒推编译过程。题库、速查表和交付清单属于附录性材料，不要求写成叙事正文。

## 2. 中文表达

- 第一次出现时使用「中文术语（英文/API 名称）」，后文优先使用中文；
- 句子尽量使用「主语—动作—结果」的顺序，避免连续堆砌名词；
- 「必须、通常、可以、可能」分别表示强约束、常见做法、可选做法和不确定结果；
- 不用「魔法、神奇、闭着眼」等夸张词描述编译器行为；
- 删除没有信息增量的开头和转场，例如「在当今时代」「本文将深入浅出」「让我们一起了解」；
- 不用「最佳实践」「显著提升」「核心价值」等词替代适用条件、测量结果或具体动作；
- 「不是……而是……」只用于纠正常见误解或区分两个已经说明的概念，不把它当作普通转折；
- 正式文档可以保留正式语气，但每个结论都应能指出对象、条件、结果或限制；
- 面试表达集中放在复习附录 A，主线章节只保留与推理有关的章末问题，避免同一答案反复出现。

### 2.1 段落与标题

- 一个段落只表达一个主题，中心判断尽量放在段首；
- 一个句子尽量只保留一个主干，长句拆成「前提—动作—结果」或并列句；
- 标题说明读者将解决什么问题，不用「核心价值」「深度解析」这类空泛标题；
- 标题层级连续，代码、命令、URL 和字段名不参与自然语言润色；
- 章末通常包含一段“从本章走向下一章”和一组“实验与思考”；不为凑齐格式机械增加小结、任务与问答三件套。

### 2.2 教科书式叙述

本教程的语言定位是**教科书**：读者是来建立理解的，不是来照单执行的。为此：

- 每章开头从前章留下的疑问、一个反常现象或一个具体算子切入，不罗列难度、用时、前置和产出；
- 每个小节先写动机或过渡（为什么需要它、与上文什么关系），再进入代码或表格；
- 段与段之间要有因果或递进过渡，禁止孤立的短句堆叠；
- 描述性的项目符号列表改写为连贯段落；只有真正的并列枚举（选项、步骤、检查项）保留列表；
- 表格前用一句话说明「这张表回答什么问题、怎么看」；
- 允许使用「你/我们」的第一人称叙述旅程，克制、不煽情；
- 比喻允许但必须准确，且随后落回技术事实；
- 参考资料放在相关结论附近或章末，不把链接堆进章首导航；
- 章末先收束本章改变了什么认识，再提出下一章的问题；实验与思考单独置于其后，不让验收清单代替结尾。

## 3. 代码与实验

- `可运行示例`：有必要的导入、输入、调用和正确性检查；
- `内核骨架`：重点展示结构，省略部分宿主代码时必须说明；
- `伪代码`：只用于表达算法或时序，不能让读者误以为可以直接运行；
- 每个 tensor 都要在声明处标注逻辑 shape：TileLang 参数使用 `T.Tensor((...), dtype)`，
  shared/fragment 使用 `alloc_*((...), dtype)`，PyTorch 张量在创建行末补 `# shape: [...]`；
  标量和索引不需要伪装成张量，但应在必要时注明其作用域；
- 任何性能数字都附带硬件、版本、输入形状、dtype、warmup、rep 和计时方式；
- 任何 tile 示例都至少说明整除尺寸、尾部尺寸和 padding/guard 的处理方式。
- 结果错误时先缩小形状、固定随机种子、对照参考实现，再看生成代码；不要直接从复杂优化版本猜原因。
- 性能实验只改变一个主要变量；报告中同时保留基线、变体、观察证据和下一步假设。

## 4. 术语边界

- 「全局内存读取量」指逻辑上的 load 次数；除非说明，否则不等同于实际 HBM 流量；
- 「同步」描述调用完成后的可见语义；「等待」描述异步操作的完成状态，两者不混用；
- 「布局 swizzle」指共享内存/片段布局变化；「光栅化 swizzle」指 block 调度顺序变化；
- 「自动」表示编译器可能推断或插入，不表示所有后端和版本都保证相同结果。

### 4.1 外部资料的证据边界

- 官方文档和当前源码用于确认 API 与实现；论文用于解释设计，不替代最新接口说明；
- release、discussion 和 issue 用于发现版本变化与失败边界，引用时写明版本和日期；
- 社区文章用于补充学习路径与工程取舍，不单独支撑通用性能结论；
- issue 中已经关闭的问题可以保留为测试设计案例，但不能写成当前版本仍然存在；
- 外部性能数字必须保留硬件、软件、shape、dtype 和计时范围，否则只作为阅读线索。

## 5. 章末实验与思考

章末保留“实验与思考”，但它不承担正文叙事。实验至少产生一个可验证结果：代码、边界测试、
生成代码片段、性能表、排障记录或口述说明。问题应要求解释证据或迁移概念，只复述定义不算
完成章节。

## 6. 交付前检查

按以下顺序检查每篇教程：

1. **结构**：读者是否知道先做什么、为什么做、做完如何判断成功；
2. **表达**：段落是否只有一个中心，是否把抽象名词改成对象、动作和结果；
3. **事实**：代码、公式、版本、数字、限制和引用是否与原材料一致；
4. **可复现性**：命令、输入、环境和验证方法是否足够让读者重做实验；
5. **承接**：本章的产物是否真的会在下一章被使用，而不是只在小结里重复。
