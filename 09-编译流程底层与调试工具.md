# 第 09 章 编译流程底层与调试工具

一个 TileLang 内核从 Python 写法到设备指令，要经过多次 lowering。多数时候，这条流水线
隐藏在一次调用背后；只有当编译失败、数值错误或性能异常时，我们才意识到“源代码”与“实际
执行的程序”之间还隔着许多层表示。

调试的关键不是掌握最多工具，而是选择离故障最近的观察层。生成期错误应追踪 IR 在哪个 pass
首次失去合法性；数值错误要回到最小输入，检查索引、边界与中间状态；性能异常则必须观察生成
指令、时间线和硬件计数器。三类问题可能表现得相似，却需要完全不同的证据。

本章从这三类故障出发，反向重建 TileLang 的编译过程。IR trace、生成源码、`T.print`、
Compute Sanitizer 与 Nsight 会在需要它们时出现，而不是作为一排工具名称依次介绍。最终目标
是形成一种诊断习惯：先定位问题属于哪一层，再用最小代价取得能够排除假设的证据。

> **本章导航** 高级难度，预计 3–5 小时；前置是第 04～08 章，且至少完成过一个可复现的编译
> 和运行实验。IR/source 调试可先离线做，Compute Sanitizer/Nsight 需要 CUDA GPU。学完你会
> 留下一个最小复现、一次 lower trace，以及按问题类型组织的排障记录。参考：
> [TileLang Tools Overview](https://tilelang.com/tools/index.html)、
> [Debugging Tile Language Programs](https://tilelang.com/tutorials/debug_tools_for_tilelang.html)、
> [Compute Sanitizer](https://docs.nvidia.com/compute-sanitizer/ComputeSanitizer/index.html) 和
> [Nsight Compute](https://docs.nvidia.com/nsight-compute/)。

## 9.1 三个抽象层次（学习视角，不是固定 API 分层）

TileLang 允许你在不同深度上介入编译，从「只写计算逻辑」到「逐条发射 PTX」。下面这张表用
三个层次概括了你和编译器分别负责什么，以及每一层适合谁：

| 层次 | 用户控制 | 编译器负责 | 适合谁 |
|---|---|---|---|
| L1 | 只写计算逻辑 | 自动调度、tiling、优化 | 通用用户；复杂融合（如 FA）自动调度常不够灵活 |
| L2 | tiling、共享内存、流水线 | 布局推断、线程映射、流水线编排 | 多数生产内核（本课程主体） |
| L3 | 线程级原语、PTX/内联汇编 | 几乎不干预 | 性能专家 |

这三个层次只是定位问题的学习工具：先在 L1/L2 解决算法和 tile 结构，再在 L3 处理
目标架构明确要求的 warp/PTX 细节。不要因为能下探到 L3，就跳过前面的正确性和测量。

## 9.2 编译流水全景

```text
@T.prim_func 的 Python 函数
        │  (解析成 TIR，即 TVM 中间表示)
        ▼
TIR（含 T.copy / T.gemm / T.Pipelined 等"高层 tile 指令"）
        │  渐进式 lowering（一连串 pass，见 9.3）
        ▼
device IR（已展开为循环、线程绑定、同步、mma/ptx 原语）
        │  codegen
        ▼
CUDA / HIP / Metal /（LLVM IR 或 C for CPU）
        │  nvcc / hipcc / 其它
        ▼
cubin / 可执行模块  ← kernel(A,B,C) 直接调用
```

关键认知：**TileLang 是「渐进式 lowering」**。例如 `T.copy(global, shared)` 先被
`LowerTileOP` 展开成 `T.Parallel` 循环，再被展开成线程级循环 + 向量化访存，最后
才变成 CUDA 的 `ld.global`/`cp.async` 等语句。因此报错时：

1. 先在 **Python/TIR 层观察 IR**，确认形状、索引、作用域和高层 tile 算子是否正确；
2. 不要在尚未定位问题层次时直接检查 C++ 源码。

## 9.3 核心 pass 一览（以官方文档为准）

渐进式 lowering 由一连串 pass 接力完成，每一圈都把 IR 往硬件更近一步。下面这张表列出主线
上最常见的几个 pass 和它们的职责：

| pass | 职责 |
|---|---|
| `LowerTileOP` | 把 tile 级算子（T.copy 等）展开为可并行循环 |
| `LegalizeSafeMemoryAccess` | 自动插入越界保护；证明安全时消除 |
| `LayoutInference` | 推断 fragment/shared 的线程布局（第 07 章） |
| `PipelinePlanning` / `InjectSoftwarePipeline` | 把 `T.Pipelined` 变成 prologue/steady/epilogue + 异步拷贝 |
| `InjectTmaBarrier` | TMA 拷贝的 mbarrier 编排 |
| `ThreadSync("shared")` | 需要时插入 `tvm_storage_sync("shared")` |
| codegen | 生成目标源码（CUDA/HIP/...） |

这些 pass 和命名会随版本演进。某些开关可通过 `pass_configs` 暴露；
`tilelang.transform` 下的直接 pass 调用也属于进阶/版本敏感用法，先用当前安装版本
的 `dir()`/官方文档确认，不要把本表当作稳定 ABI。

## 9.4 三类问题与排查入口

不同的问题要选择不同的观察层，否则容易在错误的抽象层上反复试错。下面这张表回答「问题属于
哪一类、它通常长什么样、第一步该用什么工具」：

| 类别 | 表现 | 第一排查工具 |
|---|---|---|
| 生成期（lowering 失败） | 编译报错、codegen 报错 | 打印 IR、`TL_LOWER_TRACE` |
| 正确性 | 跑通但结果错 | `T.print`、参考实现对比、回调打印源码 |
| 性能 | 结果对但远低于峰值 | `do_bench`、生成源码审查、Nsight Compute |

### 9.4.1 性能差：先用根因分类表定位修复位置

如果你已经测过 tile、stages、线程数和 swizzle 这些开关，性能仍上不去，就别再无目标地扩大
参数空间。先对照下面的根因表，判断要改的是代码、编译策略，还是重新选择优化方向。

"Correct but Slow" 研究（[arxiv 2607.04454](https://arxiv.org/abs/2607.04454)，A100/GH200
上 22 个 Triton/TileLang 内核实测）把「正确但慢」的根因按**修复位置**分成三类，每一类都有
独立的硬件计数器特征。这张表是本章性能排查的索引——先对号入座，再决定改代码还是换编译策略：

| 编号 | 根因 | 修复位置 | 计数器/证据特征 | 典型幅度 |
|---|---|---|---|---|
| RC0a | 归约写成 `T.serial` 而非 `T.reduce` | **用户代码** | `warp_stall_long_scoreboard` 很高、`warp_stall_barrier` ≈ 0（等内存 ≠ 等同步） | 修复可恢复 1347× |
| RC0b | 缺向量化 load（如 bf16 逐个标量 load） | 编译器 codegen | `l1tex` bytes/sector 低、sector/request 高 | — |
| RC1 | strided 访问退化为标量 LD，`cp.async` 不触发 | 编译器 codegen | load 效率仅 ~12.5%（conv 案例） | 修复约 2.2× |
| RC2a | autotune 空间缺维度（L2-swizzle、num_warps、num_stages） | **调优配置** | TFLOPS 对扫参曲线：扩空间后抬升（16384² GEMM 59.7%→81.9%） | 约 1.37× |
| RC2b | 工作集远超 L2，无缓存分块 | 库成熟度 | L2 命中率低（49.4% vs cuBLAS 80.5%）、DRAM 吞吐高 | — |
| RC3 | 寄存器溢出导致 occupancy 降低 | 编译器 codegen | 每线程寄存器 254、occupancy 12.4%、local load/store spill 数十 GB | — |
| RC4 | 无算法级多样性（如 Winograd） | 库成熟度 | SM 利用率差，但贡献最小（约 2–3%） | 小 |

这张表有三个直接用法：

1. **归约慢**先查 RC0a：把 `T.serial` 归约换成 `T.reduce`（`dim` 指明归约维度），
   并保持输入输出用原生 dtype——这是用户侧能修的最大单项。
2. **大 GEMM 慢**先查 RC2a/RC2b：扩 autotune 空间 + 单独开 `T.use_swizzle` 实验，
   看 L2 命中率是否变化。
3. **RC0b/RC1/RC3 属于编译器 codegen**，用户能做的通常是改写法诱导（显式向量化提示、
   改 tile/布局、换 dtype）；修不好就记录复现步骤和版本，归档为结构性残留。

### 官方案例：向量化报错

```text
ValueError: Check failed: lanes <= 4 (8 vs. 4) : Ramp of more than 4 lanes is not allowed.
```

含义：某处在代码生成阶段产生了 8 lane 的向量访存（硬件/后端不支持）。先打印
codegen 前的 IR，定位是哪个 `T.copy`/`T.Parallel` 的向量宽度被推断成 8，再决定
是否加 `coalesced_width=` 提示或改布局。

## 9.5 调试手段详解

### 9.5.1 `T.print`：内核内打印

`T.print` 适合确认某个中间值是否已经错误，不适合替代参考实现，也不适合测量性能。
当内核能够运行但结果错误，并且问题可能来自某个中间量（如 max、sum 或某一行）时，应先
缩小输入，并限制打印到一个 block、一个 lane 或一个标量。输出格式和支持范围以当前目标后端为准。

```python
T.print(C_f, msg='accumulator:')    # buffer 自动单线程打印，防刷屏
T.print(A_shared, msg='A tile: ')
T.print(C[0], msg='C[0] = ')
if i == 0:
    T.print(s, msg='scalar:')
```

输出形如：`msg='hello' BlockIdx=(0,0,0), ThreadIdx=(0,0,0): 0`。

### 9.5.2 查看/改写生成源码

源代码看不出明显问题、结果却出错时，就去翻生成源码。TileLang 是高层描述，只有生成源码
能确认 `T.copy`、`T.gemm` 最终对应哪些指令；分析性能异常时，同样要看生成的是
`cp.async`、`mma` 还是标量循环：

```python
source = kernel.get_kernel_source()     # 最终 CUDA/HIP 源码
print(source)

# 进阶：某些版本提供 codegen 后处理回调；先查当前版本再使用
from tilelang.engine.callback import register_cuda_postproc_callback

@register_cuda_postproc_callback
def tilelang_callback_cuda_postproc(code, _):
    print(code)
    return "// patched\n" + code          # 记得返回修改后的代码
```

### 9.5.3 IR Lower Trace：看每个 pass 改了什么

编译报错时，应先确认 IR 从哪个 pass 开始出现异常，而不是直接查看最终生成源码。IR Lower
Trace 会展示各个 pass 前后的 IR 差异，帮助定位问题首次出现的位置：

```bash
TL_LOWER_TRACE=terminal python my_script.py   # 终端 diff
TL_LOWER_TRACE=html     python my_script.py   # 生成 HTML diff 报告
TL_LOWER_TRACE=both     python my_script.py
```

或只对单 pass 聚焦对比：

```python
# 对单个函数运行 lower trace，观察指定 pass 前后的 IR 变化。
from tilelang.tools import lower_trace as lt
# results 保存每个 pass 的输入/输出或差异信息，具体结构以版本为准。
results = lt.lower_trace(func, tilelang.transform.ThreadSync("shared"))
```

### 9.5.4 Pass Visualizer：结构化视图

当文字 diff 难以展示布局推断或流水线编排等结构变化时，可以使用 Pass Visualizer。它按 pass
分组展示变化，比逐行检查 diff 更容易观察整体结构：

```bash
# 对 GEMM 示例生成 pass 可视化页面，并固定问题尺寸和 tile 参数。
python -m tilelang.tools.pass_visualizer.viewer \
    tilelang/tools/pass_visualizer/examples/gemm_relu.py \
    --set M=1024 --set N=1024 --set K=1024 \
    --set block_M=128 --set block_N=128 --set block_K=32 \
    --out gemm_relu_passes.html
```

产出自包含 HTML：左栏 pass 列表（changed/no-op），右栏 SBlock 结构树，tile 算子 /
同步原语 / 硬件指令分色高亮——适合研究布局推断、流水线这类**结构型** pass。

### 9.5.5 AutoDD：自动最小化复现

遇到能够稳定复现的编译错误时，可以使用 AutoDD 删除复现脚本中的无关代码，得到最小复现，
便于本地排查和提交 issue：

```bash
# 让 AutoDD 在错误信息稳定的前提下自动缩小复现文件。
python -m tilelang.autodd examples/autodd/tilelang_buggy.py \
  --err-msg "T.gemm K shape check failed" -o minimized.py
```

先让 bug 稳定复现，再用 AutoDD 自动删代码直到最小复现集，最后跑
`minimized.py` 验证。

### 9.5.6 Compile-Only：没有设备也能检查到哪一步

手头没有 GPU、或者只想确认「能不能 lowering 到 codegen 之前」时，我就用它——它替我走到
生成源码这一步，省下真机编译的时间：

```bash
python -I -m tilelang.tools.compile_only \
  --target cuda --output_file out.cu example.py
```

它会选择脚本中第一个 `@tilelang.jit` kernel 或 `PrimFunc`，执行 lowering 并写出目标源码，
但不做设备编译和运行。适合在无 GPU 环境中复现解析、pass 和 codegen 前问题，也适合在 issue
中附上最小生成结果。限制同样明确：它不能发现运行期越界、竞态、数值误差或真实性能问题。

### 9.5.7 Analyzer：估算工作量，不能替代 profiler

尚未判断内核受访存还是算力限制时，可以先用 Analyzer 估算理论工作量，形成 roofline 假设。
`tilelang.tools.Analyzer` 能从 TIR 估算部分 FLOPs 和 global-memory 字节数，但它只做静态
估算，不能替代 profiler。当前官方文档说明，它的 FLOPs 识别主要覆盖 `T.gemm`，内存统计
主要覆盖跨函数参数（全局 buffer）边界的 `T.copy`。elementwise 算术、缓存命中、bank conflict、
同步和实际指令吞吐不能从这个静态结果直接推出。

于是 Analyzer 的输出适合回答“理论上大约做了多少已识别计算/搬运”，Nsight 和真实计时
才回答“设备实际上怎样执行”。两者不一致时，检查统计口径、lowering 后访问和缓存行为。

### 9.5.8 其它

下面这几个工具我不常碰，但各自对应一类「前面的手段已经不管用」的情形，知道什么时候
该轮到它们就够：

- **Compute Sanitizer**：CUDA 运行期优先用 `compute-sanitizer --tool memcheck python my_script.py`
  查越界/非法访问，再用 `--tool racecheck` 查共享内存竞态；工具本身会显著放慢程序，
  先缩小输入形状。
- **Nsight Systems / Nsight Compute**：前者看时间线、传输和并发，后者看单 kernel 的
  memory/compute throughput、stall、occupancy 等指标；不要用一个工具替代另一个。
- **布局可视化**：把 fragment 的线程/本地索引映射画成图（PNG/PDF/SVG），怀疑
  “数据摆放不对”时使用；图解释映射，不直接证明性能；
- **Pass Diff**：旧工具仍可做选定 pass 的前后比较，但官方已经建议优先使用覆盖完整
  lowering 的 `TL_LOWER_TRACE`；
- **IKET**：面向 CUDA kernel marker、range 和 payload 的高级诊断工具。先有明确的时间线
  问题再启用，不作为入门排障第一步。

## 9.6 排查决策树

前面把工具拆开讲了一遍，真遇到问题时我更愿意顺着下面这棵树走——它回答的始终是三件事：
错误发生在编译期、运行期、还是只在性能上？每一步该往哪一层看？

```text
报错/结果错？
├─ 编译期报错 → 打印当前 IR（改 T.copy 前/后）→ 定位越界/宽度/布局问题
│     ├─ 无设备 → compile-only 固化生成结果
│     └─ 还不行 → TL_LOWER_TRACE=html 看哪个 pass 引入了问题
├─ 运行期结果错 →
│     ├─ 用 T.print 抽查中间值（max/sum/某一元素）
│     ├─ 与 torch 参考逐块对比（缩到最小形状验证）
│     └─ 用 postproc 回调把源码打出来，人工审索引
└─ 性能差 →
      ├─ Analyzer 提理论假设，do_bench 确认带宽/TFLOPS 差距
      ├─ 看生成源码：合并访问？bank conflict？流水线在不在？
      └─ 上 Nsight Compute（warp stall、吞吐）收尾
```

## 9.7 本章回顾

这一章把调试从「对着报错猜」变成「沿着 lowering 一层层看」。编译 = prim_func → TIR →
pass 序列 → CUDA 源码 → 可执行，「渐进式 lowering」是理解一切报错的钥匙。三类问题
（生成/正确性/性能）各有主打工具：IR/`TL_LOWER_TRACE`、`T.print`+参考实现、profiler+
生成源码。同时要清楚工具的边界：compile-only 只覆盖生成路径，Analyzer 只做有限静态估算，
两者都不能替代设备测试。最后，`register_cuda_postproc_callback`、AutoDD 和 IKET 用于已经
收敛到特定层的问题，而非排查的第一步。

## 9.8 动手任务

在进入第 10 章之前，请完成下面任务，把这条排障流程在真实错误上走一遍：

1. 人为制造一个尾部尺寸错误，分别用参考实现、生成源码和最小形状定位；
2. 开启 `TL_LOWER_TRACE=terminal` 或 `html`，指出哪个阶段首次引入了关键变化；
3. 对一个性能异常内核写出「假设—测量—修改—结果—结论」五行记录；
4. 说明什么时候应该使用 Compute Sanitizer/Nsight，而不是继续猜 pass 名称。
5. 在无 GPU 模式下运行一次 compile-only，并说明它没有验证哪些运行期性质。

## 9.9 自问自答

下面这些问题用来检验你是否能把这一章的知识讲出来（详答见第 10 章）：

1. **TileLang 编译流程？**（phrase：TIR → 渐进式 lower → codegen；可画图）。
2. **`LegalizeSafeMemoryAccess` 干什么？**（越界保护自动插桩/消除）。
3. **如何定位「结果错」的内核？**（T.print 抽查 + 参考实现分块对比 + 审源码）。
4. **`get_kernel_source()` 说明你理解什么？**（抽象层与硬件的映射，加分项）。
5. **LayoutInference 失败一般长什么样？**（布局冲突报错；解法：annotate_layout 显式指定）。
6. **性能差第一步看什么？**（判断 bound 类型 → 带宽/TFLOPS 实测 → 审访存模式）。
